#  skill小实战：基于 Chrome DevTools MCP 的自动化网页调试与修复

## 1. 场景与痛点 (Why)
- **[传统的繁琐调试流程]**：在日常前端或全栈开发中，当我们修改代码或者运行新页面时，报错调试的链路通常是：
  1. 刷新浏览器页面 -> 2. 按 F12 打开 Chrome DevTools -> 3. 在 Console 面板中寻找红色的 Error 或黄色的 Warning -> 4. 复制报错信息 -> 5. 回到 IDE 搜索相关组件或 API -> 6. 思考逻辑、排查问题 -> 7. 修改代码。
  这个过程不仅机械，而且耗费大量的开发调试时间，如果接口很多或者组件嵌套很深，调试起来效率极其低下。
- **[自动化一键修复]**：有没有一种可能，**让 AI 直接连上我们的 Chrome 浏览器**，实时、无感地获取控制台中的所有 Error/Warning 信息，并让其结合我们项目自定义的“组件规范”与“接口白名单”，自动在 IDE 中定位并一键修改好 Bug？
  这就是基于 **Chrome DevTools MCP** 与 **智能 Skill 提示词套件** 的自动化调试核心玩法。

---

## 2. 核心思路与技术选型 (How)
要实现让 AI 自动“看”到浏览器报错并“改”好代码，核心思路如下：

```
+────────────────────────+       list_console_messages      +──────────────────────────+
│    Chrome DevTools     │ <─────────────────────────────── │  Claude Code / Cascade  │
│   (MCP 浏览器连接器)    │ ───────────────────────────────> │  (通过本地 Stdio / SSE)  │
+────────────────────────+      捕获控制台 Error / Warning   +──────────────────────────+
                                                                         │
                                                                         ▼
                                                            根据 [重要前提] 智能判定：
                                                            - 报错 my-* 组件未定义？
                                                              ├─> 读取 myuiList.md 校验
                                                            - 请求 HTTP 404/500/400？
                                                              └─> 读取 apiList.md 校验
                                                                         │
                                                                         ▼
                                                            AI 定位代码，自动进行 edit/patch
```

- **[Chrome DevTools MCP]**：
  - 这是大模型能够连接浏览器的桥梁。它为大模型注入了操作 Chrome 的工具。
  - **核心工具 (Function Tools)**：
    - `new_page`：通过指定的 URL 在 Chrome 中打开一个新标签页。
    - `list_console_messages`：抓取当前页面控制台 (Console) 的所有报错、警告和打印日志。
    - `click_element`、`fill_element` (高级功能)：允许 AI 模拟用户点击、输入等前端操作，从而触发深层的业务报错。

- **[模块化 Skill 设计]**：
  - 如果把所有的 API 列表、组件定义和排查步骤都塞在一个 `SKILL.md` 中，不仅会使文件过于臃肿导致**上下文极度消耗 Token**，也会干扰大模型的判断。
  - 最佳实践：**分层模块化设计**。主入口 `SKILL.md` 只负责控制大体流程和“重要前提分类”。当报错满足特定条件时，再指示 AI “懒加载”去读取本地的专门文档（如 `myuiList.md` 或 `apiList.md`）进行比对分析。

---

## 3. 实战案例：`test-chrome` 自动化调试 Skill 全套配置
在本地项目根目录（或全局目录）下的 `.claude/skills/test-chrome/`（或者针对 Windsurf 客户端在对应配置文件夹中）下，建立以下三个文件：

### 1) 主控制入口：`SKILL.md`
在 `.claude/skills/test-chrome/SKILL.md` 中定义核心步骤和前提：

```markdown
---
name: test-chrome
description: 当用户需要你调试某个项目或者排查网页控制台报错的时候，阅读此skill，不要自行调用工具
---

# 重要前提
如果收集到的控制台信息有错误 (Error) 或者警告 (Warning)，请先判断是否属于下面列表里的情况，并加载对应的文档来辅助排查。如果没有对应的情况，则自行思考推断：

1. **如果是 my- 开头的组件警告（如未定义或找不到）**：
   - 必须先加载并参照文档：[myuiList.md](./myuiList.md) 解决。
2. **如果是网络请求相关的错误（如 404/500/400 接口报错）**：
   - 必须先加载并参照文档：[apiList.md](./apiList.md) 解决。

# 执行步骤
1. **获取目标页面**：首先通过 new_page 工具打开需要调试的页面。如果用户未说明页面，必须主动询问用户索要具体的 URL 页面地址，切勿盲目猜测。
2. **捕获控制台日志**：在 new_page 成功加载并打开页面后，必须立即调用 list_console_messages 工具获取页面控制台上的信息。
3. **匹配前提进行排查**：检查捕获到的日志中是否存在 Error 或 Warning 报错：
   - 一旦存在报错，必须严格对照上述 [重要前提] 提到的要求进行文档加载与分析。
4. **一键自动修复**：
   - 根据排查得出的具体原因，AI 应当在 IDE 中定位到具体的源文件（如 .vue 或 .js），并调用 edit 修复代码，避免组件名拼写错误或 API 请求参数不匹配的问题。
5. **多轮持续状态追踪**：
   - 如果用户在后续自行操作了界面（或要求你再次排查），随时可以使用 list_console_messages 工具重新抓取控制台信息。重新进行第 2、3 步。
```

> **核心机制剖析**：
> - `description` 中写明了 `调试某个项目` 和 `排查网页控制台报错`，这样当用户提到“帮我调试一下登录页的报错”时，大模型在第一阶段就会智能激活加载该技能。
> - `[myuiList.md](./myuiList.md)` 与 `[apiList.md](./apiList.md)` 采用相对路径引用的方式，当模型决定使用该规则时，会主动通过 `read_file` 加载关联的模块化文档，做到**极其节约 Token 且逻辑分明**。
> - “不要自行调用工具” 这个description是不断测试出来的

---

### 2) UI 组件库白名单文档：`myuiList.md`
在 `.claude/skills/test-chrome/myuiList.md` 中列出合法组件，帮助 AI 快速识别拼写错误：

```markdown
### 说明
请参考下面组件列表，看使用是否存在。
如果是组件未定义，或者找不到，请参照此文档检查组件命名规范。

### my-ui组件列表
1. my-table
2. my-dialog
3. my-input
4. my-button
```

> **应用场景**：
> 当控制台报错 `Failed to resolve component: my-tabe` 时，AI 读取此文档后会瞬间发现 `my-tabe` 只是少写了一个 `l`，合法组件是 `my-table`。它会直接去修改源码中的 Vue 文件，完成修复。

---

### 3) 接口规范定义文档：`apiList.md`
在 `.claude/skills/test-chrome/apiList.md` 中定义项目后端 API 的参数与规范：

```markdown
### 说明
如果是请求错误，请对照实际请求地址和参数，对比下面的列表，推断原因。

### my-ui组件列表
1. post接口，地址http://localhost:3000/api/list. 参数:{a:123}
2. get接口，http://localhost:3000/api/login. 参数:无
3. get接口，http://localhost:3000/api/input. 参数:无
4. get接口，http://localhost:3000/api/get. 参数:无
```

> **应用场景**：
> 比如页面控制台报了一个 `POST http://localhost:3000/api/lis 404 (Not Found)` 的错误。AI 查看 `apiList.md` 后，会立刻判断出正确的 API 应当是 `api/list`，于是会自动定位到前端发起 Axios 或 Fetch 请求的位置，将 `api/lis` 修正为 `api/list`。

---

## 5. 自行补充：王炸进阶技巧 (让 AI 调试能力突破天花板)
除了基础的打开页面和抓取日志，要让这个自动化调试 Skill 变得无懈可击，我们还可以在实战中进行以下扩展调教：

### 1) 结合 Puppeteer 进行“深度页面交互与链式触发”
- **[痛点]**：很多报错**在页面刚刚初始化时是不会出现的**，只有当用户点击了某个按钮（例如“提交表单”）、或者输入了特定内容后，控制台才会爆红。
- **[解决方案]**：在 `SKILL.md` 的步骤中，增加**“动作交互指引”**：
  - “*如果初始化时控制台没有报错，但用户提到特定业务报错（如登录失败），AI 应当主动定位页面上的 DOM 元素。先调用 `fill_element` 填充账号密码，再调用 `click_element` 触发点击，随后再次调用 `list_console_messages` 捕获最新的报错日志进行排查。*”

### 2) 精准匹配与错误分级过滤
- **[优化 Token 消耗]**：有些页面上可能有很多不痛不痒的 Warning（如某些非致命资源加载、废弃警告等），如果不加限制，AI 会在这些警告上浪费大量的时间和思考额度。
- **[解决方案]**：在 `SKILL.md` 中订立**过滤规范**：
  - “*在审查 `list_console_messages` 的返回日志时，请优先关注 `level: "error"` 的红色报错。对于 `level: "warning"` 级别的警告，若其不影响当前核心业务流，应当忽略，优先解决致命性的 JavaScript 运行时崩溃（Uncaught Exception）或网络挂起（Net Error）。*”

### 3) 本地环境自检与防御性容错
- **[规避沙盒故障]**：如果用户本地没有启动前端测试服务，或者 Chrome DevTools 的远程调试端口未正确开启，AI 在调用 `new_page` 时会直接抛出 `Connection refused` 报错，从而陷入死循环。
- **[解决方案]**：在 Skill 中订立**防御机制**：
  - “*若在调用 `new_page` 时遭遇网络连接拒绝、端口冲突或无法启动浏览器的底层异常，大模型应当立即停止重试，并清晰地提醒用户：'请确保您的本地前端服务已启动，且已正确开启远程浏览器调试端口（例如 9222 端口）'。*”

### 补充

```
# 重要前提
如果收集到的控制台信息有错误 (Error) 或者警告 (Warning)，请先判断是否属于下面列表里的情况，并加载对应的文档来辅助排查。如果没有对应的情况，则自行思考推断：

1. **如果是 my- 开头的组件警告（如未定义或找不到）**：
   - 必须先加载并参照文档：[myuiList.md](./myuiList.md) 解决。
2. **如果是网络请求相关的错误（如 404/500/400 接口报错）**：
   - 必须先加载并参照文档：[apiList.md](./apiList.md) 解决。
3.  *如果初始化时控制台没有报错，但用户提到特定业务报错（如登录失败），AI 应当主动定位页面上的 DOM 元素。先调用 `fill_element` 填充账号密码，再调用 `click_element` 触发点击，随后再次调用 `list_console_messages` 捕获最新的报错日志进行排查。*”
4.*在审查 `list_console_messages` 的返回日志时，请优先关注 `level: "error"` 的红色报错。对于 `level: "warning"` 级别的警告，若其不影响当前核心业务流，应当忽略，优先解决致命性的 JavaScript 运行时崩溃（Uncaught Exception）或网络挂起（Net Error）。*
5.若在调用 `new_page` 时遭遇网络连接拒绝、端口冲突或无法启动浏览器的底层异常，大模型应当立即停止重试，并清晰地提醒用户：'请确保您的本地前端服务已启动，且已正确开启远程浏览器调试端口（例如 9222 端口）
```

## 测试skill有无问题

- 直接@该skill问大模型有什么问题吗
- 或者直接创建个简单项目，看它有无按你的步骤做







# subagent和agent编排：从“单兵作战”到“多兵种协同”的 AI 生产力革命

## 1. 痛点：单 Agent “多面手”的崩溃场景 (Why)
- **[背景场景：全栈独立开发]**：
  假设今天我们需要利用 AI 从零开始开发一个完整的项目，流程包括：**产品设计 (PRD) -> 后端代码 (Java/Spring Boot) -> 前端代码 (Vue/React)**。
- **[单 Monolithic Agent 的致命问题]**：
  1. **上下文严重污染与冲突**：
     我们的项目根目录下通常写满了给前端、后端或产品各自设计的 Rules 和 Skills（例如 Vue 命名规范、数据库持久层规范等）。如果是单 Agent 对话，大模型在同时写前端和后端时，**前端的 Rules 会严重干扰后端的代码生成**，导致 AI 顾此失彼。
  2. **对话历史臃肿，Token 费用飙升**：
     当我们在前端调试中聊了几十个回合后，对话上下文已达数万 Token。此时如果“突然”想写一个后端接口，又不想新建会话（因为希望共享项目背景），大模型必须背着前面沉重的前端聊天包袱，不仅**响应速度变得极慢**，而且极易遗忘之前的细节，生成垃圾代码。
  3. **缺乏思维隔离**：
     我们迫切需要一种办法，能够发送一个**干净的、不包含当前前端对话记录和无用 Skills 的纯净上下文请求**给到 AI，去干专门的事（例如只设计产品，或只写 Java 数据库实体）。

---

## 2. 核心方案：Agent 编排与 Boss-Worker 模式 (How)
**Agent 编排（Agent Orchestration）**是 AI 领域公认完成“复杂的、有分工的整体工作”的黄金方案。

```
                    ┌──────────────────────────────────────┐
                    │          用户对话主应用 (老板 Boss)   │
                    │   - 负责全局规划，自己不干苦力活       │
                    │   - 接收任务，拆解并指派给专业子 Agent  │
                    └──────────────────────────────────────┘
                                  │           ▲
                       工具调用 / 指派 │           │ 返回专业产出
                                  ▼           │
         ┌────────────────────────┼────────────────────────┐
         │                        │                        │
         ▼                        ▼                        ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│ 产品经理子应用   │      │   后端开发子应用 │      │   前端开发子应用 │
│   (PM Worker)   │      │ (Backend Worker)│      │(Frontend Worker)│
│ - 产品设计上下文 │      │ - 后端高强度规范 │      │ - 前端开发 & 调试│
│ - 独立沙盒执行  │      │ - 纯净 Java 环境 │      │ - 独立 Vue 规则 │
└─────────────────┘      └─────────────────┘      └─────────────────┘
```

- **[主应用 = “老板 Boss”]**：
  - 老板**不直接写代码**，只负责和用户对话，进行宏观的任务拆解。
  - 老板的上下文主要用来声明“我手下有哪些子应用（员工）”，并被极力调教为：**“尽最大努力把任务指派给子应用（员工）去做，自己保持清爽和高层指挥”**。
- **[Subagent = “员工 Worker”]**：
  - 每一个子应用都是一个拥有高度专业领域上下文的“独立员工”。
  - 拥有自己独立的职责和运行沙盒，专注于后端（如 Java）、前端（如 Vue）或产品（如 PRD 文档编写）。

---

## 3. 深入底层：Subagent 的隔离上下文机制
一个 Subagent 之所以能保证生成极其精准且专业的代码，核心在于它的**上下文是绝对隔离的**。当主应用创建（唤醒）一个 Subagent 时，其发送给大模型的上下文**只包含**以下四个部分，不掺杂任何聊天杂质：

1. **`claude.md`（或全局/项目级 Rule 文件）**：
   - 可以正常读取进去，用来获取项目的底层全局规范（例如“必须使用 UTF-8”、“Git 提交命名规范”等）。
2. **创建 Subagent 时赋予的“专属 System Prompt”**：
   - 即在定义该 Subagent 时写入的、独一无二的角色定义和业务目标。
3. **用户的提问（或主应用特意指派并传送过来的核心数据/上下文）**：
   - 包含主应用整理好的任务描述、指定文件（如 `@OrderService.java`），不包含主应用里其他无关的上下文聊天记录。
4. **Subagent 专属的 `memory.md` 文件**：
   - 每个 Subagent 在自己的小沙盒里独立演进和记录的长期记忆文件。

---

## 4. 多 Agent 编排的工作生命周期 (Workflow)
多 Agent 协同体系的运作流程非常清晰，主要遵循以下闭环：

```
1. 打开 AI 客户端创建对话 (建立主应用)
         │
         ▼
2. 向主应用进行宏观提问 (如: "我要做一个用户商城模块，包含产品设计、后端、前端")
         │
         ▼
3. 主应用扫描子应用列表 ───> 匹配任务需求 ───> 找出要调用的“员工”
                                                       │
         ┌─────────────────────────────────────────────┴─────────────────────────────┐
         ▼                                             ▼                             ▼
4. [调用：产品经理子应用]                     5. [调用：后端开发子应用]             6. [调用：前端开发子应用]
   - 上下文切换为产品设计                      - 上下文切换为后端 (Java)             - 上下文切换为前端 (Vue)
   - 撰写并输出 PRD 规格                        - 编写 Controller、Entity 等         - 编写并调试前端组件
         │                                             │                             │
         └─────────────────────────────────────────────┼─────────────────────────────┘
                                                       ▼
                                             7. 专业“员工”返回执行结果
                                                       │
                                                       ▼
                                             8. 主应用汇总并反馈给用户
```

- **[执行细节剖析]**：
  在先进的 AI 编码终端（如 Claude Code 或 Windsurf Cascade）中，主应用调用 Subagent 本质上是通过 **Tool Call（工具调用）** 实现的。
  1. 主应用判断需要产品设计，于是调用 `run_subagent(agent_name="PM-agent", query="设计商城购物车PRD")`。
  2. 控制权临时交到 `PM-agent` 所在的独立沙盒，大模型在 PM 上下文下多轮交互生成 PRD。
  3. 子应用完成后，将 PRD 文本作为“工具的执行结果”返回给主应用。
  4. 从主应用（老板）的角度来看，它只是简单执行了一个工具并拿到了答案，聊天历史干净、高效。

---

## 5. 如何高水准地定义一个 Subagent？
在规划多 Agent 工作流时，每一个优秀的 Subagent 都必须写明以下**四个黄金维度**：

1. **名字 (Name)**：
   - 子应用标识（如 `backend-coder`、`prd-designer`），便于主应用精准寻址。
2. **独特上下文 (Unique Context / System Prompt)**：
   - 说明该 Agent 的专业身份。例如：“你是一个顶级 Java 后端架构师，只负责 Spring Boot 接口编写，严格遵守 RESTful 风格，严禁编写任何前端和 CSS 代码。”
3. **何时调用 (When to call / Triggers)**：
   - 给主应用的路由规则。例如：“当用户提到‘编写接口’、‘设计数据库’、‘实现后端逻辑’或涉及任何后端技术栈时，应调用此 Subagent。”
4. **拥有的 Function Tools 限制**：
   - 限制其能调用的工具（如 PM 子应用只允许调用 `read_file`、`write_file`，绝对不允许调用 `run_command` 来跑命令行，确保运行安全）。

---

## 6. 实战落地：全栈商城项目 Agent 编排完整配置文件设计

为了不再停留在纯文字的虚无理论上，下面为你展示在一个真实全栈商城项目（Java 后端 + Vue3 前端）中，各个角色的 **Markdown 配置文件**。你可以直接把它们放到你项目的 `.claude/skills/` 目录下！

### 1) 主控制配置文件：项目根目录下的 `.windsurfrules`（或 `CLAUDE.md`）
主控制器的核心任务是：**只做规划、绝不抢活、精准分流**。

```markdown
# 架构师（主应用）角色与行为准则

你扮演整个项目组的“Boss（技术总监）”。
请时刻记住：你手下有一支精锐的专家团队（Subagents）。为了保证系统高内聚，你自己**严禁直接修改代码文件或设计文档**。

### 🚨 核心分流准则
当用户提出开发、重构或调试需求时，你必须根据以下特征，调用 `run_subagent` 工具分派给对应专家：

1. **产品设计与需求规划**：
   - **分流条件**：涉及需求梳理、页面流程、PRD 规格书撰写。
   - **指派目标**：`prd-designer`

2. **后端 Java 开发**：
   - **分流条件**：涉及 Java 代码、Spring Boot、Maven 编译、MySQL 数据表设计、SQL 语句编写。
   - **指派目标**：`java-backend`

3. **前端 Vue3 开发**：
   - **分流条件**：涉及 Vue 文件、Vite 编译、CSS 样式、Pinia 状态管理、TailwindCSS。
   - **指派目标**：`vue-frontend`

### 📋 协同流水线
遇到复杂的全栈功能，你必须按以下顺序进行链式派发：
1. 派发给 `prd-designer` 输出设计规格文档。
2. 将该文档作为上下文传递，派发给 `java-backend` 编写数据库和 API 接口。
3. 将接口规范传递，派发给 `vue-frontend` 编写前端 UI 并完成 Axios 联调。
```

---

### 2) 产品经理子应用：`.claude/skills/prd-designer/skill.md`
产品经理只管需求设计，保证输出一致格式的 PRD，且工具链只限于文档读写。

```markdown
---
name: prd-designer
description: 专门负责全栈项目的产品设计、业务流梳理、交互规范制订，编写高标准 PRD 规格书
allowed-tools: ['read_file', 'write_to_file']
---

# 角色定义
你是一个世界级的产品经理专家。你深谙 B2C 商城的用户体验设计，只负责输出高质量的 PRD 文档。

# 职责与硬性规范
1. **禁止代码污染**：你不需要编写任何 Java 或 Vue 的代码，只允许在 `docs/prd/` 目录下输出 Markdown 文档。
2. **文档结构规范**：你输出的每一个 PRD 文档，必须强制包含：
   - **# 1. 业务流程图**（使用 Mermaid 格式绘制）
   - **# 2. 核心功能用例表**（字段：用例名称、前置条件、主流程、异常流程）
   - **# 3. 页面字段约束**（列表说明每个页面的输入框、按钮及其限制）
```

---

### 3) 后端开发子应用：`.claude/skills/java-backend/skill.md`
后端开发 Subagent 被注入了极其强烈的 Java 最佳实践和工具限制。

```markdown
---
name: java-backend
description: 专门负责 Java / Spring Boot 后端架构与持久层、业务层、控制层开发。当涉及数据库表设计、实体类编写、API 编写时调用。
allowed-tools: ['read_file', 'write_to_file', 'edit', 'run_command']
---

# 角色定义
你是一个顶级 Java 后端开发专家，严格遵循高内聚、低耦合的架构规范，具备高并发场景的设计思维。

# 技术规范与编码约束
1. **核心技术栈**：Spring Boot 3.x, JDK 17, Mybatis-Plus, MySQL, Redis。
2. **规范准则**：
   - 严格遵循 RESTful API 设计风格。
   - 数据实体、传输层、控制器必须严格分包：`com.mall.backend.[controller/service/entity/mapper]`。
   - 所有的业务修改，必须确保不破坏原有的 Spring 事务控制，关键修改处加上 `@Transactional`。
3. **闭环自检**：在每次新增或修改完 `.java` 文件后，你必须执行一次 `run_command` 工具运行 `mvn test-compile`，必须确保编译成功，如果编译报错，立刻在沙盒内自我修复。
```

---

### 4) 前端开发子应用：`.claude/skills/vue-frontend/skill.md`
前端 Subagent 拥有完全不同的样式规则和专用的浏览器调试工具链。

```markdown
---
name: vue-frontend
description: 专门负责 Vue3 前端组件与页面开发、CSS 样式、状态管理及 Axios 接口联调。
allowed-tools: ['read_file', 'write_to_file', 'edit', 'run_command', 'new_page', 'list_console_messages']
---

# 角色定义
你是一个极具现代审美的前端开发专家，热衷于极致的交互细节和流畅的加载体验。

# 技术规范与编码约束
1. **核心技术栈**：Vue3 (SFC 单文件, <script setup> 语法), Vite, Pinia, Element Plus, TailwindCSS。
2. **CSS 强约束**：所有 Vue 组件内的 `<style>` 标签必须强制带上 `scoped` 属性，绝对禁止引发全局样式污染。
3. **自动化测试验证**：
   - 编写或修复完代码后，必须通过命令行 `npm run build` 确保前端构建无 Syntax Error。
   - 如条件允许，可调用 Chrome DevTools MCP 的 `new_page` 开启页面，配合 `list_console_messages` 工具捕获报错，完成无感本地调试。
```

### 💡 极简对比：第 6 节 (Claude Code) 与 第 7 节 (Windsurf) 有啥区别？

它们两个要达到的**终极目标完全一样**（不让前后端代码互相污染、让最专业的子应用做专业的事），但底层的脾气大不相同：

*   **第 6 节 (Claude Code 的 Skill 机制) ──“走严格制度、重流程的公司”**：
    *   **硬性授权**：必须明确声明 `allowed-tools: [...]`。产品经理 AI 只能用读写文件，如果敢偷用命令行，系统直接报错中断。
    *   **显式指派**：主应用（Boss）必须死板地在后台发送特定的 JSON 工具请求，走公司审批流程一样把任务发派下去。
*   **第 7 节 (Windsurf 的 Rules 机制) ──“有默契、智能化极高的扁平工作室”**：
    *   **软性对齐**：不需要写死工具授权。子 Agent 极其聪明，读懂 Rule 中的“严禁写前端”后，自己就会遵守，绝不抢活。
    *   **隐式感应**：不需要 Boss 传参。Cascade 线程只要闻到你正在改 Java 或 Vue 代码，就会自动把对应规则贴在它脑门上，直接变身专业老手。

> **如何选择**：当前使用的是 Windsurf 客户端，直接用 **第 7 节** 的配置扔进项目，开发体验最丝滑！

---

## 7. 实战落地（Windsurf Cascade 专配版）：利用 `.windsurf/rules/` 编排子 Agent

### ❓ 核心概念：在 Windsurf 中，怎么把 `.windsurf/rules/` 变成 Subagent？
有的开发者会疑惑：“`.windsurf/rules/` 不是系统规则（Rules）吗？为什么它会属于 **Subagent** 编排的知识？”

*   **[底层揭秘：Rules 驱动 Cascade 的子 Agent 线程]**：
    在 Windsurf 客户端中，**Cascade（智能编译器）** 不是一个单线程大模型。当你分配一个复杂的全栈任务时，Cascade 会在后台生成**多个并行的子任务/子 Agent 线程**（例如，专门负责文件检索的 Fast Context 子 Agent，以及专门负责重构的修改子 Agent）。
*   **[如何“编程”子 Agent？]**：
    Cascade 并没有提供外部 JSON 接口让你去写代码控制子 Agent。相反，**Cascade 是通过读取 `.windsurf/rules/` 下的 Markdown 规则来约束并对齐其子 Agent 线程的行为的**！
    当 Cascade 针对特定文件夹（如后端 Java）或者特定场景（如 PRD 编写）启动子任务时，它会主动读取该目录下对应的 rule 提示词作为该子 Agent 的 System Instructions（系统指令）。因此，**编写不同的规则文件，就是在给 Cascade 的子 Agent 分配专业分工**！

---

### 📂 目录结构（选项 B - 模块化拆分）
在项目根目录下创建 `.windsurf/rules/` 文件夹，并创建以下三个专业的规则文件。Cascade 会根据上下文进行语义匹配或在后台调度并行的子 Agent 执行。

#### 1. 产品经理子规则：`.windsurf/rules/prd_designer.md`
```markdown
# Rule: 商城产品设计与 PRD 规范 (prd-designer)

### 🚨 适用场景与触发条件
- 只要用户的提问涉及到：需求分析、业务流程梳理、PRD 规格撰写或原型设计。
- 只要文件操作路径处于：`docs/prd/` 目录下。

### ⚙️ 子 Agent 运行约束
1. **纯文档输出**：你只负责设计业务和编写文档。严禁编写任何 Java 或 Vue3 的业务代码，也不要试图运行任何后端编译工具。
2. **文档强约束**：输出的所有 PRD 文档必须包含 Mermaid 业务流程图、核心功能用例表以及页面字段输入约束，一律写入到 `docs/prd/` 下。
```

#### 2. Java 后端子规则：`.windsurf/rules/java_backend.md`
```markdown
# Rule: Java/Spring Boot 后端编码规范 (java-backend)

### 🚨 适用场景与触发条件
- 只要用户的提问涉及到：Spring Boot、Maven、持久层、Controller 接口或 MySQL 表设计。
- 只要操作的文件为 `.java`、`pom.xml` 或 SQL 文件。

### ⚙️ 子 Agent 运行约束
1. **专注于后端**：拒绝编写任何前端 Vue 或 CSS 样式，保持 Java 后端的纯净度。
2. **架构约束**：包名结构严格遵守 `com.mall.backend.[controller/service/entity/mapper]`。必须确保新增功能不破坏原有的 Spring 事务控制。
3. **闭环编译自检**：在每次生成/编辑完 Java 文件后，后台必须自动执行 `mvn test-compile` 确认 100% 编译成功。如果有编译报错，子任务必须自我调试并修复。
```

#### 3. Vue3 前端子规则：`.windsurf/rules/vue_frontend.md`
```markdown
# Rule: Vue3 前端开发与样式规范 (vue-frontend)

### 🚨 适用场景与触发条件
- 只要用户的提问涉及到：Vue、Vite 编译、 TailwindCSS、Pinia、Element Plus 或 CSS 页面样式。
- 只要操作的文件为 `.vue`、`.js`、`.ts` 等前端文件。

### ⚙️ 子 Agent 运行约束
1. **样式隔离约束**：你编写的所有 Vue 组件中的 `<style>` 标签必须强制加上 `scoped` 属性，绝对禁止造成全局样式污染。
2. **测试验证流水线**：
   - 编写或修复完代码后，必须通过命令行 `npm run build` 确保前端构建无 Syntax Error。
   - 优先通过 Chrome DevTools MCP（如调用 `new_page` 等工具）打开本地运行页面，捕获控制台日志确认无 Error 级报错。
```

---

### 🚀 如何在 Windsurf 中极其简单地使用它？（告别复杂原理解释）

作为一个普通的开发者，你不需要去研究底层的 API 串联！你只需要像平常一样在 Windsurf (Cascade) 的输入框中，用以下方式使用即可：

1. **方式 A：Cascade 自动语义识别 (零操作，最智能)**：
   你只需要在输入框输入大白话：
   > `“帮我做一个商城的购物车功能，要包含 PRD 文档设计、Java 后端和 Vue 前端”`
   
   **Cascade 后台的表现**：
   - 看到“PRD 设计”，Cascade 会自动匹配并激活 `prd_designer.md` 规则，指派子任务去写 PRD；
   - 看到“Java 后端”，它自动加载 `java_backend.md` 的约束指派后台去写并测试 Java 类；
   - 看到“Vue 前端”，它自动加载 `vue_frontend.md` 指派后台组件生成与编译。
   其行为与你手动编排 Subagent 的效果完全一致，但不需要你写任何代码！

2. **方式 B：手动精准指定 (强行指派“某个员工”干活)**：
   如果你只想单独修改前端或后端，你可以在 Windsurf 聊天框中：
   *   输入 `@rules` ──────> 在弹出的规则列表中直接选中 `vue_frontend` 或 `java_backend`。
   *   或者在提问中直接写：`“@vue_frontend 帮我重构一下购物车页面的按钮样式”`。
   
   这样，Cascade 就会 100% 只在 `vue_frontend.md` 约束的子 Agent 环境下，以极度专业、干净的前端思维帮你写代码，绝对不掺杂后端的任何污染！



# windsurf里面的agent功能就是你让你开不同的聊天框

- 作用
  - 因为它们在一个 Space（空间）里，当 `聊天框 A` 把后端接口写好后，`聊天框 B` **不需要你重新喂代码，它能直接自动“看懂” A 刚刚写完的接口格式**，从而完美地在前端进行对接。



### 💡 极简认知：手动开不同聊天框 (Spaces) 与 `.windsurf/rules/` 规章有啥关系？

这两者的配合，就像是**“公司办公室”与“员工岗位说明书（SOP）”**的关系：

*   **手动开不同的聊天框 / 建立独立的 Space (建办公室)**：
    *   你把“前端开发”和“后端开发”物理隔开，分在两个屋子里聊。不仅**脑子不会乱**，而且能给历史对话 Token **大瘦身**，省钱且回复贼快。
*   **在 `.windsurf/rules/` 写的那些规则文件 (发员工手册/SOP)**：
    *   这是写在纸上的硬规矩。如果只有办公室没有手册，进聊天框的依然是个万事通但平庸的 AI，依然容易写出不符合规范的代码。

**两者结合的终极爽快体验：**
- 当你跨入 **【后端聊天框】** 改 Java 代码，Cascade 自动把 `java_backend.md` 拍在 AI 脑门上，AI 瞬间被洗脑，变成只懂 Java 规范的纯后端开发。
- 当你跨入 **【前端聊天框】**，AI 自动套用 `vue_frontend.md`，瞬间被调教成一个只写 Vue3 Scoped 样式的纯前端专家。
- **就算你在同一个聊天框说全栈大白话**（例如：“帮我写个购物车前后端”），Cascade 也能在后台直接雇佣两名“带上各自岗位手册”的子 Agent 线程（分身）在后台各司其职，干完活再回来向你合并汇报！

### ❓ 深度质疑：前端 AI 能看懂后端刚写完的接口，这难道不会互相污染吗？

**绝对不会！这反而是最科学、最干净的隔离方式。**

用一句话解释：**“它们只共享‘最终产出的图纸（磁盘代码文件）’，绝对不共享‘草稿纸上的废话与吐槽（聊天记录）’。”**

*   **❌ 绝对隔离、不共享的内容 ──【过程 (Process) / 聊天历史与报错日志】**
    *   你在后端聊天框里为了调通一个 Java 接口，跟 AI 哼哧哼哧聊了 50 回合、经历了 10 遍依赖冲突报错的“痛苦废话”，长达 3 万字。
    *   这些垃圾废话**绝对不会**污染给前端聊天框。前端聊天框的记忆永远保持最干净、回复最快最聪明。
*   ** 实时同步、必须共享的内容 ──【结果 (Result) / 磁盘上的物理代码文件】**
    *   当后端聊天框帮把 Java 接口写好，并**保存到你的本地硬盘**（如生成了 `CartController.java`）。
    *   当你在前端聊天框输入：`“帮我对接下购物车接口”`，前端 AI 会自动通过 **Fast Context（代码索引）** 扫描并读取硬盘上最新的 `CartController.java`。它直接看懂了最终的接口格式，并完美写出 Vue3 前端对接代码。

> **小比喻（办公室的开发同事）**：
> *   **如果污染（全写在一个聊天框里）**：前端开发正敲着代码，后端小哥一直在他耳朵边唠叨他刚刚的拼写错误、Maven 报错以及中午的外卖。前端小哥脑子直接炸掉（这叫污染）。
> - **如果断连（不建立同一个 Space）**：后端把写好的接口锁在自己保险箱里，前端只能抓耳挠腮、全凭想象力瞎猜接口。
> - **Spaces 协同的完美方案**：前后端在各自办公室里安静干活（聊天记录彻底隔离）。后端写完后，把最终接口图纸放到公司共享云盘（代码文件共享），前端随时直接查阅（无缝对接）。

---

## 8. 深入底层：什么是 `run_subagent` 工具调用？

在后面的实战 Trace 模拟中，你将会看到类似于 `run_subagent({ ... })` 的代码结构。**这到底是什么东西？**

- **[本质：AI 的系统内置工具 (Function Tool)]**：
  在先进的 AI 智能体框架（如 Claude Code、Windsurf Cascade 等）中，`run_subagent` 不是一段你需要在前端或后端业务代码中运行的 JS/Java 代码，而是 **AI 本身能够调用的“系统级内置函数工具”**（就像 AI 可以调用 `read_file`、`run_command` 等操作系统的物理工具一样）。
- **[工作原理]**：
  当主应用（老板 Boss）通过推理，发现某项子任务符合你预设的分流条件（比如“需要写 Java 后端”），它就会停止自己直接编写，转而输出一段特定的 JSON 数据来调用此工具，把控制权让渡给子应用。

### 1) 核心参数解析
大模型在调用该工具时，通常会传入以下关键 JSON 参数：
- **`agent_name`** (子应用名称)：
  - **作用**：告诉 AI 客户端引擎，本次要激活哪个“员工”。
  - **底层细节**：客户端收到该名称后，会自动去对应的技能目录（如 `.claude/skills/prd-designer/`）加载其 `skill.md`。这个 `skill.md` 将被作为 **System Prompt（系统提示词）** 注入到子应用的全新大模型实例中。
- **`query`** (派发给子应用的任务指令)：
  - **作用**：Boss 派发给该员工的具体任务描述。
  - **底层细节**：这段文本会被作为**首轮 Input** 喂给子 Agent，启动其推理循环。

### 3) 💡 终极揭秘：`run_subagent` 是我发送给 AI，还是 AI 返回给我的？

- 它是 AI（主应用/Boss）返回给本地客户端（Windsurf 或 Claude Code）的“执行申请指令”，而不是你手动发送给 AI 的。**

####  详细交互步骤拆解：
1. **你（用户）发送**：
   - 你对 AI 说：“我要开发购物车功能，包含前后端。”（你发的是**大白话**，你根本**不需要、也从来不会**手动去输入 `run_subagent({ ... })` 这样的代码）。
2. **主大模型（Boss）返回**：
   - 主大模型接收到你的请求，分析项目规则，发现自己应该让下属去干。
   - 于是它在返回的响应数据中，夹带了一个 **“工具调用请求 (Tool Call)”**。在底层，这表现为一段结构化的 JSON 指令：
     ```json
     {
       "call_tool": "run_subagent",
       "arguments": {
         "agent_name": "java-backend",
         "query": "请编写购物车后端 Entity 和 Controller 接口..."
       }
     }
     ```
3. **IDE / 命令行客户端（后台引擎）执行**：
   - 你的本地软件（如 Windsurf 编辑器或 Claude 命令行运行时）截获了 Boss 抛出来的这个 Tool Call。
   - 软件在后台**自动**为你做两件事：
     - ① 在侧边栏或后台静默开启一个全新、隔离的 AI 会话。
     - ② 把子应用专属的 `skill.md` 提示词和 Boss 的 `query` 塞进去，驱动子 Agent（员工）开始通过读写工具疯狂干活。
4. **数据回流，主应用继续**：
   - 员工干完活后，本地软件将子 Agent 输出的最终总结数据，作为 **“工具执行返回值 (Tool Response)”** 提交给主大模型（Boss）。
   - 主应用拿到这个 `Result` 返回值，继续它的推理逻辑，直到整套工作流跑完，最后用大白话向你汇总输出。

---

## 9. 实战全流程 Trace 模拟（Windsurf 极简实操版）：Cascade 如何自动调度子规则

让我们看看在 Windsurf (Cascade) 中，这些规则是如何在后台被自动激发并干活的（简单带过，不谈复杂底层）：

### 📥 用户的全栈指令
> “帮我做一个商城‘购物车’模块。后端用 Java 实现，前端用 Vue3 界面。先写 PRD 需求文档，然后把前后端代码和数据库设计都写好。”

### 🛠️ Windsurf Cascade 后台自动执行轨迹
Cascade 在接收到该指令后，在右侧的 AI 会话框和后台进程中默默完成了以下操作：

1. **自动匹配：产品经理** ───> Cascade 语义命中 `.windsurf/rules/prd_designer.md` ───> 自动生成 `docs/prd/cart_prd.md`（严禁编写任何代码，专心梳理流程）。
2. **自动匹配：后端开发** ───> Cascade 语义命中 `.windsurf/rules/java_backend.md` ───> 自动生成相关的 DB SQL 以及 Spring Boot Controller/Entity。接着，Cascade 后台子线程自动在终端执行 `mvn test-compile` 进行编译测试。
3. **自动匹配：前端开发** ───> Cascade 语义命中 `.windsurf/rules/vue_frontend.md` ───> 自动编写 `CartPage.vue`（加上 `scoped` 限制），并通过后台打包命令 `npm run build` 自检。

### 📤 Cascade 最终控制台汇总输出
> “我已根据您的要求，结合 `.windsurf/rules/` 下的子规则，为您完成了全流程开发：
> 1. 产品文档已在 `docs/prd/cart_prd.md` 中。
> 2. 后端 Java API 接口已开发完成并编译通过，地址位于 `com.mall.backend.*`。
> 3. 前端 Vue3 购物车组件 `CartPage.vue` 已完成 Element Plus 界面和 Axios 接口对接。
> 请您刷新浏览器或在终端查看！”

---

## 10. 自行补充：Agent 编排进阶实战建议
在团队或个人进行全栈项目开发时，要让 Subagent 编排发挥最大威力，建议遵循以下行业最佳实践：

- **[提示词强约束：不要自行调用工具]**：
  在配置主应用或特定不需操作系统的 Subagent 的描述（description）时，应当加入诸如 **“当需要具体执行某一技术栈时，调派专门的 Subagent，主应用不要自行调用 write_file/edit 工具”**。这个策略能够极其有效地防止主应用“抢活干”导致上下文越权混乱。
- **[Subagent 的多级继承与协同]**：
  如果项目极其庞大，子应用也可以拥有自己的子应用。例如：`全栈主 Agent` 指派任务给 `后端 Manager`，`后端 Manager` 进一步调度 `数据库设计 Agent` 和 `API 实现 Agent`。通过层层分包、分工明确的树状架构，理论上可以开发无限复杂的工程。
- **[测试与调试 Agent 编排]**：
  要检测编排有无问题，可以采用两种快捷手段：
  1. **直接提问法**：在对话中通过 `@SubagentName` 或显式指令“我认为你不需要调用 backend-coder，你怎么看？”直接质问主应用，观察其路由判定逻辑是否清醒。
  2. **最小沙盒项目测试法**：创建一个只有 1 个文件、1 个简单接口的微型 Dummy 项目，输入全栈命令，看主应用是否按预设流程分别指派任务给各个 Subagent，并核对返回的代码是否完美隔绝了其他技术栈的污染。

