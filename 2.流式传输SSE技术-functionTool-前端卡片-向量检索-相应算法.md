# 基础ai对话项目的优化 -- 流式传输

### 之前后端直接返回json，优化为流式传输

#### 优势（为什么要做流式）

- 解释
  - **更快的“首字延迟”体验**
    - JSON：必须等模型整段回答生成完，接口一次性返回
    - 流式：模型边生成边推送，前端可以逐字/逐段渲染
  - **更适合长文本输出**
    - 输出越长，“等待整段返回”的体感越差，流式能显著改善体验
  - **更容易做“打断/停止生成”**（成熟产品常见）
    - 前端断开 SSE 连接即可停止接收（服务端也能感知并中止）

#### 什么时候用 JSON，什么时候用流式

- 使用 JSON（一次性返回）更合适
  - 输出很短（如分类/打标/判断）
  - 需要一次性拿到结构化结果（JSON Schema 等），中途不希望渲染半成品
  - 接口链路里还要做较多后处理（比如要等多个工具调用完成后再返回）
- 使用流式更合适
  - 需要“像打字一样”的对话体验（聊天/写作/代码生成）
  - 输出较长、或不确定会输出多久
  - 想实现“边生成边展示、随时停止”



## SSE技术

#### 解释

##### 1）SSE 是什么：基于 HTTP 的“单向推送”

- 解释
  - SSE（Server-Sent Events）是一种**服务端 -> 浏览器**的推送协议
  - 底层仍然是 **HTTP 长连接**：客户端发起一次请求，服务端不立刻结束响应，而是持续往响应里写入数据
  - 非常适合大模型：模型边生成边推送，前端边接收边渲染（“打字机效果”）

##### 2）SSE 和 JSON 一次性返回 / WebSocket 的区别

- 解释
  - JSON 一次性返回
    - 一次请求一次响应
    - 简单，但必须等到“最终答案”才能展示
  - SSE
    - 一次请求，多次推送（服务端多次 `res.write`）
    - 更轻量，足够支撑“流式输出”场景；但它是**单向**（服务端 -> 客户端）
  - WebSocket
    - 全双工（客户端与服务端都能随时发消息）
    - 更适合强实时双向交互；只做“流式输出”时往往 SSE 更省事

##### 3）SSE 消息格式：`data:` + 内容 + 空行（`\n\n`）

- 解释
  - SSE 事件是一行行文本构成的
  - 最常用字段是 `data:`，后面一般放字符串；工程里通常放 `JSON.stringify(...)`
  - 一条事件必须以空行结束（也就是 `\n\n`），客户端才会触发一次 [onmessage](cci:1://file:///c:/Users/MJL/Desktop/ai%E5%AD%A6%E4%B9%A0/ppt%E5%92%8C%E6%BA%90%E7%A0%81/1-9%20%E6%B5%81%E5%BC%8F%E4%BC%A0%E8%BE%93/code/aiclient/src/api/index.js:15:8-17:9)
- 代码（对应你后端的写法：`ppt和源码/1-9 流式传输/code/server/index.js`）

  ```js
  // 必须以 data: 开头，并以 \n\n 结束
  res.write(`data:${JSON.stringify(resObj)} \n\n`)
  ```

#### 前端接收：原生 `EventSource` vs `fetch-event-source`

- 解释
  - 原生 `EventSource(url)`
    - 优点：浏览器内置
    - 限制：只能 `GET`，自定义 header / 传 body 不方便
  - `@microsoft/fetch-event-source`
    - 本质仍然是 SSE（服务端响应仍是 `text/event-stream`）
    - 但客户端发起请求走 `fetch`：支持 `POST`、自定义 header、JSON body（与你的项目实现一致）

## 实战

### 大概过程

### 1）接口侧把调用大模型的地方加上 `stream: true`

- 解释
  - `openai.chat.completions.create({ stream: true })` 会返回一个可迭代的流
  - 你需要 `for await...of` 逐个拿到 chunk
- 进一步解释（为什么这里能写 `for await (let chunk of llmres)`）
  - `await` **不只**能写在“发网络请求”前面
  - `await` 的本质：等待一个 **Promise** 兑现
  - `for await...of` 的本质：用来遍历 **异步可迭代对象**（AsyncIterable）
    - 同步遍历用：`for (const x of iterable)`（iterable 提供 `Symbol.iterator`）
    - 异步遍历用：`for await (const x of asyncIterable)`（asyncIterable 提供 `Symbol.asyncIterator`）
  - 在本项目里：
    - `await openai.chat.completions.create({ stream: true })` 这一步返回的 `llmres` 不是“最终回答字符串”，而是一个**可以不断产出 chunk 的异步流对象**
    - 所以你后面才能用 `for await...of` 一段段消费它（每次循环拿到一个 `chunk`）
  - 常见误解澄清
    - 误解：`await` 只能写在网络请求前
    - 实际：任何返回 Promise 的事情都可以 `await`，例如：读文件、定时器、数据库、以及这里的“从流里等下一段数据”
- 代码（`ppt和源码/1-9 流式传输/code/server/index.js`）

  ```js
  const llmres = await openai.chat.completions.create({
      model: "qwen-plus",
      messages: [
          {
              role: "system",
              content: systemString
          },
          ...singleConvertList
      ],
      stream: true
  })s
  
  let resObj = {
      role: "assistant",
      id: "",
      content: ""
  }
  for await (let chunk of llmres) {
      const delta = chunk.choices[0].delta // 流式传输不是message，而是delta
      // delta.content 是本次增量内容
      resObj.id = chunk.id
      resObj.content += delta.content
      res.write(`data:${JSON.stringify(resObj)} \n\n`) // The writable.write() method writes some data to the stream, and calls the supplied callback once the data has been fully handled
  }
  //for of循环结束，才会执行到下一行
  // fs.writeFileSync("./chunkList.json", JSON.stringify(chunkList))
  //每次回答，存到singleConvertList，保存上下文
  singleConvertList.push(resObj);
  writeConversation(conversationObj)
  res.write(`data:${JSON.stringify({ done: true })} \n\n`)
  res.end()
  
  ```

#### `for await...of` 的等价理解（帮助你形成直觉）

- 解释
  - `for await...of` 你可以把它理解成：每次循环都在“等下一段数据到来”
  - 更底层一点，它相当于不断调用异步迭代器的 `next()` 并 `await` 结果
- 代码（伪等价写法，帮助理解；项目里并不需要这样写）

  ```js
  const it = llmres[Symbol.asyncIterator]();
  while (true) {
      const { value, done } = await it.next();
      if (done) break;
      const chunk = value;
      // ... 处理 chunk
  }
  ```

### 2）前端用 `@microsoft/fetch-event-source` 订阅 SSE

- 解释
  - `fetchEventSource` 是一个更好用的 SSE 客户端封装
    - 原理代码解释
  - [onmessage](cci:1://file:///c:/Users/MJL/Desktop/ai%E5%AD%A6%E4%B9%A0/ppt%E5%92%8C%E6%BA%90%E7%A0%81/1-9%20%E6%B5%81%E5%BC%8F%E4%BC%A0%E8%BE%93/code/aiclient/src/api/index.js:15:8-17:9) 会持续收到服务端推送的事件
- 代码（`ppt和源码/1-9 流式传输/code/aiclient/src/api/index.js`）

  ```js
  import { fetchEventSource } from "@microsoft/fetch-event-source"
  
  export function requestLLM(keyword, userId, convertId, callback) {
      fetchEventSource("http://localhost:3000/llm", {
          method: "POST",
          headers: {
              // 因为我们请求时是 json，这样后端才能解析 body
              "Content-Type": "application/json",
              auth: "asdasdasdas"
          },
          body: JSON.stringify({ // 注意axios不用加JSON.stringify，因为内置了，但你这是原生而不是aixos
              keyword,
              userId,
              convertId
          }),
          onmessage(event) {
              callback(event)
          }
      })
  }
  ```

### 详细过程

### 后端

### 1）正常定义接口（这里是 `POST /llm`）

- 解释
  - 流式一般用 `POST` 更舒服（参数放 body，避免 query 太长）
  - 需要 `app.use(express.json())` 才能拿到 `req.body`
- 代码（`ppt和源码/1-9 流式传输/code/server/index.js`）

  ```js
  // 设置解析请求体，这样才能拿到 req.body
  app.use(express.json())
  
  app.post("/llm", async (req, res) => {
      const { keyword, userId, convertId } = req.body;
      // ...
  })
  ```

### 2）修改响应头为 SSE

- 解释
  - `Content-Type: text/event-stream` 告诉客户端这是 SSE
  - `keep-alive` 保持连接不断开
- 代码（`ppt和源码/1-9 流式传输/code/server/index.js`）

  ```js
  res.writeHead(200, {
      'Content-Type': 'text/event-stream; charset=utf-8',
      'Cache-Control': 'no-cache',
      'Connection': 'keep-alive',
  });
  ```

### 3）用 `res.write` 按固定格式一段段写入（而不是一次性 `res.json`）

- 解释
  - 每个 chunk 的核心增量内容在：`chunk.choices[0].delta.content`
  - 服务端做了一个“累加对象”`resObj`：
    - `id` 固定为当前流的 `chunk.id`
    - `content` 每次拼接 `delta.content`
    - 每次都把“最新的完整对象”推给前端
- 代码（`ppt和源码/1-9 流式传输/code/server/index.js`）

  ```js
  let resObj = {
      role: "assistant",
      id: "",
      content: ""
  }
  
  for await (let chunk of llmres) {
      const delta = chunk.choices[0].delta
      resObj.id = chunk.id
      resObj.content += delta.content
      res.write(`data:${JSON.stringify(resObj)} \n\n`)
  }
  ```

### 4）流式结束：写入 done 信号 + `res.end()`

- 解释
  - for-await 循环结束，说明模型生成完了
  - 这时：
    - 把完整回答写入会话记录（持久化）
    - 给前端发一个 `{ done: true }` 作为结束标记
    - `res.end()` 关闭 SSE
- 代码（`ppt和源码/1-9 流式传输/code/server/index.js`）

  ```js
  // 每次回答，存到 singleConvertList，保存上下文
  singleConvertList.push(resObj);
  writeConversation(conversationObj)
  res.write(`data:${JSON.stringify({ done: true })} \n\n`)
  res.end()
  ```

### 前端

### 1）每次收到 SSE 消息，把 `event.data` 解析成对象

- 解释
  - 服务端写的是 `data:${JSON.stringify(resObj)}`
  - 前端 `event.data` 就是这段 JSON 字符串
- 代码（`ppt和源码/1-9 流式传输/code/aiclient/src/views/Home.vue`）

  ```js
  requestLLM(inputvalue.value, '001', route.query.convertId, (event) => {
      const assistantObj = JSON.parse(event.data)
  })
  ```

### 2）用 `id` 做“同一条 assistant 消息”的定位：有则替换，无则新增

- 解释
  - 流式每个 chunk 都会推一个“同 id 的对象”
  - 前端要做的是：
    - 第一次收到该 id：push 一条 assistant 消息到 `convertList`
    - 之后每次收到该 id：找到数组里同 id 的那条，替换成最新内容（实现“逐字更新”）
- 代码（`ppt和源码/1-9 流式传输/code/aiclient/src/views/Home.vue`）

  ```js
  const assistantObj = JSON.parse(event.data)
  
  const _convertList = [...convertList.value];
  const convertIndex = _convertList.findIndex(item => item.id === assistantObj.id);
  if (convertIndex !== -1) {
      _convertList[convertIndex] = assistantObj
  } else {
      isThinking.value = false;
      _convertList.push(assistantObj)
  }
  convertList.value = _convertList;
  ```

### 3）接口处理逻辑

- 解释
  - 服务端每次 chunk 取出 `delta.content`，不断拼接到 `resObj.content`
  - 客户端根据 `id` 找到同一条消息并替换，让 UI 看起来是“一个气泡在增长”
  - 循环结束后，服务端把完整回答写入历史（会话持久化），前端刷新后可通过 `conversation/get` 拿到完整记录





# function tool

## 目标：让模型“选工具 + 传参数”，大模型就是脑子但是没手，这里当作其手

- **解释**
  - Function Tool（也叫 Tool Calling / Function Calling）的核心是：
    - 你先把“可用工具清单（函数名/用途/参数）”发给大模型
    - 模型在需要时返回 `tool_calls`（告诉你要调用哪个函数、参数是什么）
    - 服务端根据 `tool_calls` 去执行你自己的业务代码（订票/查天气/打车…）
    - （完整形态）再把工具执行结果发回模型，让模型生成最终自然语言回复

## 步骤 1）定义工具清单 `tools`（告诉模型：有哪些函数可用）

- **解释**
  - 工具清单是一个数组，每个元素描述一个工具
  - 本项目使用 `type: "function"`，并在 `function` 字段里声明：
    - `name`：函数名（模型返回的 `tool_calls.function.name` 就会用它）
    - `description`：模型什么时候应该调用
    - `parameters`：参数结构描述（用于让模型产出结构化 arguments）
  - 返回的role是tool，可以用来区分返回是content还是tool
- **代码**（`ppt和源码/2-1 function tool是什么/code/server/tools.js`）

```js
const toolList = [
    {
        type: "function",
        function: {
            name: "help_dp",
            description: "当用户需要订票的时候调用此工具",
            parameters: {
                city: {
                    type: "string",
                    description: "用户要去的城市"
                }
            }
        }
    }
]
```

## 步骤 2）把 `tools` 随着对话一起发给大模型

- **解释**
  - 你在 `openai.chat.completions.create(...)` 里传入 `tools: toolList`
  - 模型就“知道你提供了哪些工具”，从而在合适时机返回 `tool_calls`
- **代码**（`ppt和源码/2-1 function tool是什么/code/server/index.js` 的 `/llm`）

```js
const llmres = await openai.chat.completions.create({
    model: "qwen-plus",
    messages: [
        {
            role: "system",
            content: systemString
        },
        ...singleConvertList
    ],
    tools: toolList,
    stream: true
})
```

## 步骤 3）流式模式下：从 `delta` 里“拼接出完整 tool_calls”

- **解释**
  - 流式返回不是一次性给完整 JSON，而是一段段 chunk
  - `tool_calls` 在流式里也会“分片返回”，所以你必须像拼接 `content` 一样拼接：
    - `function.name`
    - `function.arguments`
  - 本项目用 `index` 来定位某一个 tool call（一个回答可能包含多个工具调用）
- **代码**（`ppt和源码/2-1 function tool是什么/code/server/index.js`）

```js
for await (let chunk of llmres) {
    const delta = chunk.choices[0].delta
    resObj.id = chunk.id
    resObj.content += delta.content || ''
    if (delta.tool_calls && delta.tool_calls.length > 0) {
        //拼接tool_calls部分
        if (resObj.tool_calls) {
            //已经是第一个以后的chunk，走拼接
            delta.tool_calls.forEach((chunkTool) => {
                const toolIndex = chunkTool.index;
                //根据index找到resObj，要拼接进去的对象
                const targetTool = resObj.tool_calls[toolIndex]
                if (chunkTool.function?.name) {
                    targetTool.function.name += chunkTool.function?.name
                }
                if (chunkTool.function?.arguments) {
                    targetTool.function.arguments += chunkTool.function?.arguments
                }
            })

        } else {
            //你还是第一个chunk,直接走赋值
            resObj.tool_calls = delta.tool_calls;
        }
    } else {
        res.write(`data:${JSON.stringify(resObj)} \n\n`)
    }
}
```

## 步骤 4）本项目当前行为：检测到 `tool_calls` 时先不往前端推（先把结构拼好）

- **解释**
  - 你在 `else` 分支里才 `res.write(...)`
  - 也就是：
    - **普通文本增量**：直接推给前端（SSE 实时渲染）
    - **工具调用增量**：先在服务端把 `tool_calls` 拼接完整（暂时不推）
- **代码**（同上：`res.write(...)` 只出现在 `else` 分支）

```js
if (delta.tool_calls && delta.tool_calls.length > 0) {
    // ... 拼接 tool_calls
} else {
    res.write(`data:${JSON.stringify(resObj)} \n\n`)
}
```

## 步骤 5）用 `/simple` 接口观察“最终拼出来的 tool_calls 长什么样”

- **解释**
  - `/simple` 也传了 `tools: toolList` + `stream: true`
  - 不同的是它最后 `console.log(JSON.stringify(resObj))`，方便你在控制台看 `resObj.tool_calls`
- **代码**（`ppt和源码/2-1 function tool是什么/code/server/index.js` 的 `/simple`）

```js
app.get("/simple", async (req, res) => {
    // ...
    const llmres = await openai.chat.completions.create({
        model: "qwen-plus",
        messages: [
            { role: "system", content: systemString2 },
            { role: "user", content: keyword }
        ],
        tools: toolList,
        stream: true
    })
    // ... 拼接 resObj（含 tool_calls）
    console.log(JSON.stringify(resObj))
    res.end();
})
```

## 步骤 6）准备工具执行入口：`toolHandleMap`（把“函数名”映射到真实业务逻辑）

- **解释**
  - 模型返回的 `tool_calls.function.name` 本质就是字符串
  - 服务端一般会做一个映射表（白名单）：只允许调用你显式声明过的函数
  - 本项目已经准备了 `toolHandleMap`，用于把工具名映射到实现函数
  - 注意：这里有一个明显的拼写不一致：
    - 工具名是 `help_dp`
    - 映射表里写成了 `hepl_dp`
    - 如果你后续真去用 `toolHandleMap[toolName]` 执行，会找不到
- **代码**（`ppt和源码/2-1 function tool是什么/code/server/tools.js`）

```js
const toolHandleMap = {
    hepl_dp() {
        //订票的具体逻辑
    },
    help_dc() {
        //打车的逻辑
    }
}
```

## 步骤 7）完整的 function tool 调用闭环应长什么样

- **解释**
  - 你现在已经做到了：
    - `tools` 传入模型
    - 流式把 `tool_calls` 拼接出来
  - 完整闭环应该是：（下面即2-2 项目：服务端执行工具 + 把工具结果再发回模型有详细过程）
    - 流式结束后检查 `resObj.tool_calls`
    - 遍历每个 tool call
    - 取出 `function.name` 和 `function.arguments`
    - `JSON.parse(arguments)` 得到工具入参
    - 用 `toolHandleMap[name]` 执行真实代码拿到 `result`
    - 构造一条 `role: "tool"` 的消息，把工具执行结果发回给模型
    - 再次调用模型，让它基于工具结果输出“给前端展示的最终答复”

## 2-2 项目：服务端执行工具 + 把工具结果再发回模型

#### 步骤 7.1）流式结束后：检查是否有 `tool_calls`

- **解释**
  - 在 `for await...of` 结束后，`resObj` 已经拼出了完整 `tool_calls`
  - 先把本轮 assistant 消息写入会话历史
  - 如果存在 `tool_calls`，就进入“执行工具”分支；否则正常返回 `{ done: true }`
- **代码**（`ppt和源码/2-2 执行Function tool/code/server/utils.js`）

```js
singleConvertList.push(resObj);
writeConversation(conversationObj)

if (resObj.tool_calls && resObj.tool_calls.length > 0) {
    const tool_calls = resObj.tool_calls
    // ... 执行工具
} else {
    res.write(`data:${JSON.stringify({ done: true })} \n\n`)
    res.end()
}
```

#### 步骤 7.2）遍历每个 tool call：按 `name` 找到方法并执行

- **解释**
  - `singleTool.function.name` 是模型选择的工具名
  - `singleTool.function.arguments` 是**JSON 字符串**，需要 `JSON.parse`
  - 执行时用映射表 `toolHandleMap[name]`（白名单思想）
- **代码**（`ppt和源码/2-2 执行Function tool/code/server/utils.js`）

```js
for (let toolIndex = 0; toolIndex < tool_calls.length; toolIndex++) {
    const singleTool = tool_calls[toolIndex];
    const name = singleTool.function.name;
    const arguments = JSON.parse(singleTool.function.arguments);
    const result = await toolHandleMap[name](arguments)
    // ... 把 result 回传给模型
}
```

#### 步骤 7.3）构造 `role: "tool"` 消息：把工具执行结果回传给模型

- **解释**
  - “工具执行结果”不是直接展示给用户，而是作为上下文喂给模型
  - 本项目构造的 `toolQueryObj`：
    - `role: "tool"`
    - `content: result`（工具执行结果字符串/JSON字符串）
    - `id: singleTool.id`（用模型给的 tool call id 进行关联）
- **代码**（`ppt和源码/2-2 执行Function tool/code/server/utils.js`）

```js
const toolQueryObj = {
    role: "tool",
    content: result,
    id: singleTool.id
}
```

#### 步骤 7.4）再次调用模型：让模型基于工具结果生成“最终回复”（递归 requestAI）

- **解释**
  - 工具结果进入 `messages` 后，模型会继续输出自然语言
  - 本项目用 `requestAI(...)` 递归调用来复用：
    - 上下文管理（`conversation.json`）
    - `tools: toolList`
    - 流式 SSE 返回
- **代码**（`ppt和源码/2-2 执行Function tool/code/server/utils.js`）

```js
await requestAI({
    userId,
    convertId,
    openai,
    res,
    queryObj: toolQueryObj
})
```

#### 步骤 7.5）工具实现：`toolHandleMap` 返回字符串/JSON 字符串

- **解释**
  - 你写的“真实业务代码”最终要返回字符串
  - 复杂结构建议 `JSON.stringify` 再返回（方便模型理解，也方便日志/排查）
- **代码**（`ppt和源码/2-2 执行Function tool/code/server/tools.js`）

```js
const toolHandleMap = {
    //function tool对应的方法逻辑，最后一定要返回一个字符串或者json字符串
    help_dp(arg) {
        //订票的具体逻辑
        return `去往${arg.city}的票，订购成功`
    },
    check_store() {
        //查询库存-给回json字符串
        return "库存如下：\n" + JSON.stringify({
            xxx: 12,
            yyy: !4
        })
    }
}
```

### 前端过滤（不展示 tool 消息/空内容）

#### 步骤 8）前端只展示 user/assistant 且 `content !== ''`

- **解释**
  - 调用 function tool 的过程你通常不希望用户看到
  - 因此前端过滤：
    - `role === 'tool'` 的消息不展示（本项目通过只写 user/assistant 分支实现）
    - `content === ''` 的“占位/中间态”不展示
- **代码**（`ppt和源码/2-2 执行Function tool/code/aiclient/src/views/Home.vue`）

```vue
<div v-if="chatItem.role === 'user' && chatItem.content !== ''" class="user-content">
    <MarkDown :content="chatItem.content">
    </MarkDown>
</div>
<div v-if="chatItem.role === 'assistant' && chatItem.content !== ''" class="assistant-content">
    <MarkDown :content="chatItem.content">
    </MarkDown>
</div>
```





# 前端卡片（2-3）

## 目标：有些 tool 不是“在服务端直接干活”，而是“让前端展示一个可交互 UI”

- **解释**
  - 常规 function tool：
    - 模型选工具 -> 服务端执行 -> 把结果再发回模型 -> 模型生成自然语言 -> 前端展示
  - 前端卡片型 tool（**本项目的做法**）：
    - 模型选工具 -> 服务端拿到“卡片需要的数据” -> **直接下发给前端**
    - 前端渲染一个**预制 UI 卡片**给用户点选
    - 用户点选后，把点选结果再当作一条新的用户消息发给 `/llm`，继续走正常对话流程

## 步骤 1）定义“前端卡片工具”：把某些 tool 名加入 `frontList`

- **解释**
  - 目前没有统一协议来告诉前端“这是卡片”，所以本项目用一个约定：
    - 如果工具名在 `frontList` 里，就按“前端卡片”逻辑处理
    - 下发给前端的消息仍然用 `role: "tool"`，但会额外带一个自定义字段 `cardName`
- **代码**（`ppt和源码/2-3 前端工具卡片/code/server/tools.js`）

```js
const frontList = ['wm_card'];
```

## 步骤 2）在 `toolList` 里声明卡片型 tool（让模型知道它能“让前端展示 UI”）

- **解释**
  - 这里新增了两个与外卖相关的工具：
    - `wm_card`：用于触发前端展示“外卖选择卡片”（并携带 `kind` 参数）
    - `buy_wm`：当用户最终确认购买某个 id 时，才真正执行下单
- **代码**（`ppt和源码/2-3 前端工具卡片/code/server/tools.js`）

```js
{
    type: "function",
    function: {
        name: "wm_card",
        description: "当用户需要点外卖的时候，调用此工具可以让前端展示一个外卖选择ui结构",
        parameters: {
            kind: {
                type: "string",
                description: "外卖的种类"
            }
        }
    }
},
{
    type: "function",
    function: {
        name: "buy_wm",
        description: "当用户决定了要点某个外卖的时候，调用此工具进行下单",
        parameters: {
            id: {
                type: "string",
                description: "要购买的外卖的Id"
            }
        }
    }
}
```

## 步骤 3）卡片数据从哪里来：卡片型 tool 的 handle 专门返回“卡片渲染所需数据”

- **解释**
  - `wm_card()` 在服务端不“回复一句话”，而是返回一个列表数据（这里写死模拟）
  - 这些数据会被服务端塞进 `arguments.data` 下发给前端
- **代码**（`ppt和源码/2-3 前端工具卡片/code/server/tools.js`）

```js
wm_card() {
    return [
        { name: "煲仔饭1", price: "12", id: 'bz1' },
        { name: "煲仔饭2", price: "22", id: 'bz2' },
        { name: "煲仔饭3", price: "32", id: 'bz3' }
    ]
}
```

## 步骤 4）服务端识别卡片型 tool：不再递归发回模型，而是直接下发给前端

- **解释**
  - 流式结束后拿到 `resObj.tool_calls`
  - 遍历 tool call：
    - 如果 `name` 在 `frontList`：
      - 执行 `toolHandleMap[name]()` 拿到卡片数据
      - 构造一个 `role: "tool"` 的对象，并额外加：
        - `cardName: name`
        - `arguments: { ...模型参数, data: 卡片数据 }`
      - **直接** `res.write(...)` 推给前端并 `res.end()`
- **代码**（`ppt和源码/2-3 前端工具卡片/code/server/utils.js`）

```js
if (frontList.indexOf(name) !== -1) {
    //按前端工具卡片逻辑处理
    //不给ai回复，直接给前端下发消息
    const result = await toolHandleMap[name](arguments)
    const obj = {
        role: "tool",
        content: "此消息无意义,纯粹让前端展示ui卡片",
        id: singleTool.id,
        cardName: name,
        arguments: {
            ...arguments,
            data: result
        }
    }

    singleConvertList.push(obj);
    writeConversation(conversationObj)
    res.write(`data:${JSON.stringify(obj)}\n\n`)
    res.end()
}
```

## 步骤 5）前端识别 `role: 'tool' + cardName`：按 `cardName` 渲染对应卡片组件

- **解释**
  - 前端把“卡片消息”也当作 `convertList` 里的一条消息
  - 渲染时额外加一段：
    - 当 `chatItem.role === 'tool'` 且 `chatItem.cardName !== ''` 时，渲染对应卡片
    - 这里用 `cardName === 'wm_card'` 映射到 `WmCard` 组件
- **代码**（`ppt和源码/2-3 前端工具卡片/code/aiclient/src/views/Home.vue`）

```vue
<div v-if="chatItem.role === 'tool' && chatItem.cardName !== ''" class="assistant-content">
    <WmCard @cardConfirm="sendToLLM" v-if="chatItem.cardName === 'wm_card'"
        :kind="chatItem.arguments.kind" :cardData="chatItem.arguments.data"></WmCard>
</div>
```

## 步骤 6）卡片组件把“用户选择”转成一句话（query），再交给原来的 `sendToLLM` 继续对话

- **解释**
  - 卡片按钮点击后，组件 `emit("cardConfirm", text)`
  - 父组件监听 `@cardConfirm="sendToLLM"`，于是会把这句话当作新的用户输入
  - 这样就回到了“和直接在输入框提问一样的流程”（图片里的流程图第 3 张）
- **代码 1：卡片组件 emit**（`ppt和源码/2-3 前端工具卡片/code/aiclient/src/components/wmCard.vue`）

```js
function dianta(id) {
    emit("cardConfirm", `我要点id为${id}的煲仔饭`)
}
```

- **代码 2：父组件复用 sendToLLM**（`ppt和源码/2-3 前端工具卡片/code/aiclient/src/views/Home.vue`）

```js
function sendToLLM(word) {
    isThinking.value = true;
    const _convertList = [...convertList.value];
    _convertList.push({
        role: "user",
        content: word
    })
    convertList.value = _convertList;

    requestLLM(word, '001', route.query.convertId, (event) => {
        const assistantObj = JSON.parse(event.data)
        // ... 追加/替换消息
    })
}
```

## 步骤 7）用户选择后：模型可能继续调用“真实执行型 tool”（如 `buy_wm`）完成下单

- **解释**
  - 当用户点选后再次进入 `/llm`，模型可能会选择调用 `buy_wm`
  - `buy_wm` 走的就是普通 tool 闭环：执行 -> 构造 `role: "tool"` 消息 -> 递归 requestAI -> 输出最终 assistant 文本
- **代码**（`ppt和源码/2-3 前端工具卡片/code/server/tools.js`）

```js
buy_wm(arg) {
    const id = arg.id
    //进行下单购买
    return `${id}商品已经下单成功，花费23.3`
}
```



## 补充：为什么上下文数据裁剪/压缩还要要额外处理 `role === 'tool'`（修复了什么问题）

- **解释**

  - 你在做“对话上下文裁剪/压缩”时，本质是在删除 `singleConvertList` 中的一段历史消息。

  - 但在 tool calling 场景里，`tool` 消息和它前后消息之间存在**强关联**：

    - `assistant` 可能发起 `tool_calls`
    - 随后你会插入一条或多条 `role: 'tool'` 消息（工具执行结果）
    - 之后模型的下一次回答，往往依赖这些 `tool` 结果

  - 如果你裁剪时“刚好把 `assistant` 截掉了，但把紧跟着的 `tool` 留下”，或者“把 `tool` 截掉了但保留了依赖它的后续回答”，就会出现上下文不一致：

    - 模型看到一个孤立的 `tool` 结果却找不到它对应的调用背景
    - 或者模型看到自己之前说了要调用工具，但工具结果不在上下文里
    - **代码 1（旧版：2-2 项目）**（`ppt和源码/2-2 执行Function tool/code/server/utils.js`）

    ```js
    if (singleConvertList.length > 10) {
        //算出来要截取多少条
        //多截取一些，方便ai接口多给我们总结一下，所以设为6，每次大于10只保留6条。
        const removeNum = singleConvertList.length - 6;
        const removeList = singleConvertList.splice(1, removeNum)
        const summaryRes = await summaryMessage(openai, removeList);
        singleConvertList.splice(1, 0, summaryRes)
    }
    ```

  - 这会导致的现象：

    - 模型答复变得莫名其妙（缺少关键中间信息）
    - 甚至继续重复调用工具、或者出现“已下单/已完成”之类的幻觉

  - 所以 2-3 做了一个最小修复：

    - 按原逻辑计算要删除的条数 `removeNum`
    - **如果删除完后下一条是 `role === 'tool'`，说明会把 tool 留在队列头部附近形成孤儿消息**
    - 就把 `removeNum` 再 +1，把这条 `tool` 也一起删掉，保证裁剪边界不会把 tool 单独留着

- **代码 2（修复版：2-3 项目）**（`ppt和源码/2-3 前端工具卡片/code/server/utils.js`）

```js
if (singleConvertList.length > 10) {
    //算出来要截取多少条
    //多截取一些，方便ai接口多给我们总结一下，所以设为6，每次大于10只保留6条。
    let removeNum = singleConvertList.length - 6;
    //如果下一条role为tool，则多截取一条
    if (singleConvertList[removeNum + 1].role === 'tool') {
        removeNum += 1
    }
    const removeList = singleConvertList.splice(1, removeNum)
    const summaryRes = await summaryMessage(openai, removeList);
    singleConvertList.splice(1, 0, summaryRes)
}
```





# 向量检索

## 目标：把“资料文本”变成向量存起来；提问时把“问题”也转向量，用相似度找回相关资料

- **解释**
  - 图片里的三步可以理解成：
    - **转化**：文本 -> embedding 向量（高维数组）
    - **存储**：把向量 + 原文（metadata）存到向量库
    - **查找**：把问题也转成同维度向量，做相似度检索，返回最相近的文本

## 补充 0）向量转化的一些常见问题（结合本项目的代码）

### 0.1 向量能转回文本吗？

- **解释**
  - 向量（embedding）是把文本映射到“语义空间里的位置”，主要用于相似度计算。
  - 你可以把它理解成“压缩后的语义特征”，不是可逆的编码。
    - 现在正在研究这个
  - 在工程上你通常不会尝试把向量还原成原文，而是：
    - 通过向量检索找回原文（metadata 里存的 `text`）
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/store.js`：原文存 metadata）

```js
metadata: {
    text: originText
}
```

### 0.2 同一个文本每次向量都一样吗？

- **解释**
  - 实际上不同 embedding 服务/模型版本/推理实现可能会导致结果存在差异。
  - 所以在一个系统里要保证：
    - 资料入库与问题检索使用同一个 embedding 模型
    - 维度一致（本项目是 `2048`）
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index.js`：资料与问题都用同模型同维度）

```js
model: "text-embedding-v4",
dimensions: 2048
```

### 0.3 向量维度（比如 64～2048）是什么意思？维度越大越好吗？

- **解释**
  - 维度可以理解成“语义空间的表达容量”。
  - 更高维通常能表达更细的差异（更精准），但：
    - 存储更大
    - 计算更慢
    - 同时你查不到相关的信息，比如只能查找到年龄，专业那些没有
  - 本项目选择 `dimensions: 2048`，并且向量库也配置成 `dimensions: 2048`，否则会维度不匹配。
- **代码**
  - **embedding 维度**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）
  - **向量库维度**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
// index.js
dimensions: 2048

// store.js
dimensions: 2048
```

### 0.4 向量大概是什么？

- **解释**
  - 在代码层面它就是一个“数字数组”。
  - 相似文本的向量在语义空间里更靠近，所以用相似度/距离就能找回相关文本。
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）

```js
const questionVectore = questionResult.data[0].embedding
```

## 补充 ）转化（文本->向量）的两种常见方案：远程 embedding vs 本地向量化

### 0.1 方案 1：用大模型/云服务做 embedding（效果好，但要 token/调用成本）

- **解释**
  - 本项目 `index.js` 直接调用 DashScope 的兼容 OpenAI embedding 接口。
  - 优点：
    - 模型能力强、语义效果通常更好
  - 代价：
    - 需要联网
    - 有调用成本（通常按 token 计费）
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）

```js
const result = await openai.embeddings.create({
    model: "text-embedding-v4",
    input: arr,
    dimensions: 2048
})
```

### 0.2 方案 2：用本地包/本地小模型向量化（不花 token，但效果可能弱一些）

- **解释**
  - 本项目 `index2.js` 用 `@xenova/transformers` 下载一个小型向量化模型，在本地做 feature extraction。
  - 优点：
    - 不依赖远程 embedding 服务
    - 没有按 token 计费的调用成本
  - 代价：
    - 本地模型语义能力可能弱于云端 embedding
    - 首次需要下载模型文件
  - 实战要点：不同模型维度不同（如 384/768），向量库 `dimensions` 要同步调整。
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index2.js`）

```js
import { pipeline, env } from "@xenova/transformers"

env.remoteHost = "https://hf-mirror.com" // 因为要配国内镜像，否则是国外的
const extractor = await pipeline("feature-extraction", "Xenova/all-MiniLM-L6-v2")
const result = await extractor(arr)
```





## 补充 ）向量如何存储：内存 / JSON / 向量数据库

- **解释**
  - 图片中提到的 3 种存储方式，在本项目都能找到对应落点：
    - 内存：不配置落盘路径，生命周期跟进程一致
    - JSON：把向量结果写入 json 文件
    - 向量数据库：用向量 DB 直接提供 insert/search

### 方案 1：直接在内存里存（进程重启就没了）

- **解释**
  - `ruvector` 的配置里有一句：
    - 没有 `storagePath` 就等于做内存储存
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
storagePath: "./data/vectorData.db"//数据按文件储存到这，没有这一项，等于做内存储存
```

### 方案 2：存 JSON 文件（适合教学/调试，不适合大规模检索）

- **解释**
  - 2-4 的 `index2.js` 展示了“把本地向量化结果写成 JSON”
  - 目录里也确实存在 `a.json`、`b.json`（说明有过落盘数据）
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index2.js`）

```js
const resultArr = Array.from(result);
fs.writeFileSync("./b.json", JSON.stringify(resultArr));
```

### 方案 3：向量数据库（推荐）：存储 + 检索一次性解决

- **解释**
  - 2-4 用 `ruvector` 的 `VectorDb`，直接提供：
    - `insert`（入库）
    - `search`（向量检索）
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
await db.insert({ /* ... */ })
const searchArr = await db.search({ vector: searchVector, k: 5 })
```

## 补充 ）相似度算法、距离与“分数”

### 相似度算法直觉：余弦相似度/欧氏距离/内积

- **解释**
  - 向量库检索本质就是计算 queryVector（即查询数据） 和每条 dataVector（向量数据库） 的“近似程度”
  - 2-4 的 `demo.js` 给了 3 个常见度量的实现示意

### 1 常见 3 种算法在本项目里的对应

- **解释**
  - 图片里提到 3 种常见算法：（之后可以去了解，你可以让ai发给你核心原理，但没必要现在）
    - Cosine：文本相似（最常见）
    - Euclidean：欧氏距离（图片/向量空间距离直觉更强）
    - DotProduct：点积（推荐/召回里也常见）
  - 2-4 的 `ruvector` 配置里就能选择这些 metric。
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
//Cosine,Euclidean,DotProduct
metric: "Cosine"//查找算法
```

### 2 距离和相似度的关系（为什么有的库返回 score 看起来越小越好）

- **解释**
  - 有些库返回的是“距离”（越小越相近），有些库返回的是“相似度”（越大越相近）。
  - 本项目的 `search()` 把 `r.score` 原样返回：
    - 你需要结合底层 metric 判断 score 的语义。
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
return {
    id: r.id,
    score: r.score,
    metadata: r.metadata
}
```

### 3 用代码理解算法（demo.js） -- 了解

- **解释**
  - `demo.js` 直接写了 3 种算法的计算方式，帮助你把“向量检索=算近似度”这件事具象化。
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/demo.js`）

```js
function euclideanDistance(vecA, vecB) {
    let sum = 0;
    for (let i = 0; i < vecA.length; i++) {
        sum += Math.pow(vecA[i] - vecB[i], 2);
    }
    return Math.sqrt(sum);
}
```





## 步骤 1）转化：调用 embedding 模型，把资料批量转向量

- **解释**
  - 2-4 项目用的是阿里 DashScope 兼容 OpenAI 的 embedding 接口
  - `input` 可以一次传多条文本，返回 `result.data[i].embedding`
  - **关键点**：你指定了 `dimensions: 2048`，后续向量库维度也必须一致
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）

```js
const arr = [
    "张三今年18岁",
    "李四今年24岁",
    "张三工资6000",
    "李四工资8000",
    "京海市最近雨季",
    "腾飞公司年利润23亿"
]

const result = await openai.embeddings.create({
    model: "text-embedding-v4",
    input: arr,
    dimensions: 2048
})
```





## 步骤 2）存储：把向量写入向量库（并把原文放到 metadata 里）

- **解释**
  - 2-4 用 `ruvector` 作为本地向量库：`VectorDb`
  - `storagePath` 配置了磁盘持久化：向量数据会写到 `./data/vectorData.db`
  - `metric: "Cosine"` 表示用余弦相似度做检索算法（最常见的一种）
- **代码 1：向量库配置**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
const db = new VectorDb({
    dimensions: 2048,//维度，
    storagePath: "./data/vectorData.db",//数据按文件储存到这，没有这一项，等于做内存储存
    //Cosine,Euclidean,DotProduct
    metric: "Cosine"//查找算法
});
```

- **代码 2：插入向量 + 原文 metadata**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
export async function add(id, vector, originText) {
    await db.insert({
        id: id,//必须,
        vector: vector,//向量数组,
        metadata: {
            text: originText
        }
    })
}
```

- **代码 3：把 embedding 结果逐条存入向量库**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）

```js
let resultData = result.data
for (let i = 0; i < resultData.length; i++) {
    await add(i + "", resultData[i].embedding, arr[i])
}
```

## 步骤 3）查找：把“问题”也转向量，并在向量库里做相似度搜索

- **解释**
  - 向量检索不能直接用“文本”去搜“向量库”
  - 必须把问题文本也转成同维度向量（这里同样 `dimensions: 2048`）
  - 然后 `db.search({ vector, k })` 返回 TopK 最相似条目
- **代码 1：问题向量化**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）

```js
const questionResult = await openai.embeddings.create({
    model: "text-embedding-v4",
    input: ["介绍一下李四"],
    dimensions: 2048
})
const questionVectore = questionResult.data[0].embedding
```

- **代码 2：TopK 检索并返回 id/score/metadata**（`ppt和源码/2-4 向量化与检索/code/server/store.js`）

```js
export async function search(searchVector) {
    const searchArr = await db.search({
        vector: searchVector,
        k: 5
    })
    return searchArr.map((r) => {
        return {
            id: r.id,
            score: r.score,
            metadata: r.metadata
        }
    })
}
```

- **代码 3：执行检索**（`ppt和源码/2-4 向量化与检索/code/server/index.js`）

```js
const searchResult = await search(questionVectore);
console.log(searchResult);
```

## 补充 1）本地向量化的方案是：不用远程 embedding，改用本地小模型

- **解释**
  - 2-4 提供了 `index2.js`：用 `@xenova/transformers` 下载一个“专门做向量化”的小模型
  - 适合：
    - 不想走远程 embedding 接口
    - 或想节省调用成本
  - 注意：不同模型输出维度不同（例如 384/768），向量库维度必须跟着调整
- **代码**（`ppt和源码/2-4 向量化与检索/code/server/index2.js`）

```js
import { pipeline, env } from "@xenova/transformers"

//env设置远程下载地址为国内镜像
env.remoteHost = "https://hf-mirror.com"

const extractor = await pipeline("feature-extraction", "Xenova/all-MiniLM-L6-v2")
const result = await extractor(arr)
```

