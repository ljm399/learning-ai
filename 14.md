# ai项目

## 记忆体系（6-8）

这一节讲的是：**AI 终端应用除了短期会话上下文，还要不要有长期记忆；如果要有，应该怎么维护。**

课件里先把记忆分成两类：

- **短期记忆**
  也就是当前会话里的对话记录，本质上就是这次聊天上下文。
- **长期记忆**
  是在长期和用户交互中，不断积累出来的用户特征、项目特征、开发偏好、历史问题等信息，并把这些内容维护在记忆文件里。

所以这节的核心不是“保存聊天记录”，而是：**让 AI 能把值得长期保留的信息沉淀下来，并在后续对话中继续利用。**

---

### 二、这节课的核心观点：记忆不是覆盖，而是自维护

这一节反复强调一个重要概念：**记忆应该是自维护的，而不是每次粗暴重写。**

也就是说：

- 不是每次都把旧记忆扔掉再重新生成
- 而是在已有记忆基础上做增量更新

课件里提到，一个好的记忆体系至少要解决下面几个问题：

1. **什么时候记**
   主要是触发时机的问题。
2. **记什么**
   哪些信息值得进入长期记忆。
3. **怎么记**
   基于哪些材料来生成记忆，记忆文件放在哪。
4. **记忆规范**
   记忆如何迭代，如何避免重复和冲突。

所以这节课真正讲的是“记忆系统设计”，不是单纯加两个读写文件工具。

---

### 三、这个项目里的记忆分层

这个项目把长期记忆分成两层：

#### 1. 项目级记忆

只针对当前项目有效。

适合记录：

- 项目结构
- 技术栈
- 已开发功能
- 项目常见 bug
- 当前项目里的用户要求

#### 2. 用户级记忆

针对用户在所有项目中的通用信息。

适合记录：

- 用户的开发偏好
- 技术背景
- 常用技术栈
- 多个项目情况
- 长期稳定的编码习惯

这和前面 RAG、command、settings 的“项目级 + 用户级”设计是一脉相承的。

---

### 四、记忆文件存在哪里

在 `memoryUtils.js` 里，当前实现把记忆文件固定为两个位置：

#### 1. 项目级记忆

```js
path.join(process.cwd(), '.front', 'memory', 'memory.md')
```

也就是：

- 当前项目目录下
- `.front/memory/memory.md`

#### 2. 用户级记忆

```js
path.join(os.homedir(), '.front', 'memory', 'memory.md')
```

也就是：

- 用户家目录下
- `.front/memory/memory.md`

`getNowMemory()` 会在读取前先检查文件是否存在：

- 不存在就自动创建目录和空文件
- 存在就直接读取内容

这说明记忆体系不是依赖数据库，而是用**本地 Markdown 文件**存长期记忆。

这种方式的优点很明显：

- 可读
- 可手工编辑
- 便于版本化和调试
- 不需要单独部署服务

对于桌面型、终端型 AI 应用来说，这种做法非常实用。

---

### 五、短期记忆在这个项目里是什么

短期记忆本质上还是当前对话历史，但课件特别强调了一点：

长期记忆的更新，不是脱离短期记忆发生的，而是要基于**最近的对话记录**。

在 `memoryUtils.js` 中，这部分由 `getHistory(maxCount = 50)` 提供。

它的逻辑是：

1. 去用户目录下的 `.front/history/<项目名>` 查历史记录
2. 找出所有 `.json` 文件
3. 按文件名时间戳倒序排序
4. 依次读取并合并
5. 最多取最近 `50` 条记录

也就是说，在这个项目里：

- **短期记忆**并不只是“当前 messages 数组”
- 还会从历史文件中抽取最近一段会话记录，作为长期记忆更新时的输入材料

这和课件里“提取最近 N 条对话记录”的思路完全一致。

---

### 六、长期记忆更新时，到底参考哪些材料

核心方法：`getMemoryContent()`

这个方法做的事情非常关键，它把“生成记忆”所需的全部材料组装成一个大 prompt。

它会读取五类信息：

#### 1. 当前项目记忆

```js
const { projectMemory, userMemory } = getNowMemory();
```

#### 2. 当前用户记忆

同上，也是从已有 `memory.md` 中读出来。

#### 3. 当前项目上下文

```js
const { projectContext, userContext } = getContext();
```

这里的项目上下文来自当前项目根目录下的 `.front.md`。

#### 4. 用户全局上下文

来自用户目录下的 `.front/.front.md`。

#### 5. 最近的历史对话记录

```js
const history = getHistory();
```

然后把这些内容全部替换进 `src/docs/memoryTemplate.md` 的占位符里：

- `${projectMeory}`
- `${userMeory}`
- `${projectMd}`
- `${userMd}`
- `${record}`

这一步非常重要，因为它说明：

**记忆不是根据当前一句话直接生成，而是基于“旧记忆 + 用户上下文 + 项目上下文 + 最近对话记录”综合推断出来的。**

---

### 七、`memoryTemplate.md` 的作用

文件：

`src/docs/memoryTemplate.md`

这个文件本质上是“生成记忆时给大模型的专用提示词模板”。

它里面主要包含几部分内容：

#### 1. 目标

明确要求模型生成两份记忆：

- 项目级记忆
- 用户级记忆

并通过 memory 工具写回。

#### 2. 记忆点

模板把应该记住的内容分得很细，主要包括：

- 项目特点
- 项目功能开发记录
- 项目常见错误和用户纠正的问题
- 用户的开发偏好
- 用户的技术情况
- 用户的项目情况

也就是说，课件不是让模型“随便记”，而是给了明确的记忆分类。

#### 3. 当前已有记忆

模板会把当前记忆内容完整注入，让模型在旧记忆基础上修改，而不是重新瞎写。

#### 4. 当前上下文

包括项目 `.front.md` 和用户 `.front.md`。

#### 5. 最近对话记录

也就是最近需要被总结进记忆的那批会话。

#### 6. 记忆规范

这部分最重要，里面明确写了：

1. 已经存在的内容不要重复写入。
2. 一定要基于已有记忆迭代，而不是彻底覆盖。
3. 如果新旧信息冲突，以新信息为准。
4. 和技术无关的内容忽略。
5. 如果变化不大，就保留原状。
6. 用户项目情况默认追加，不要轻易删除旧项目记录。

所以 `memoryTemplate.md` 其实就是“记忆生成规则书”。

---

### 八、记忆相关的两个核心工具

#### 1. `memoryGet`

文件：

`src/tools/local/memory_get.js`

作用：

- 读取当前项目级记忆
- 读取当前用户级记忆
- 返回给模型做参考

底层调用：

```js
const { projectMemory, userMemory } = getNowMemory();
```

这个工具的意义不是复杂，而是给模型一个标准入口，让它能先读当前记忆，再决定怎么更新。

课件里也强调：**如果要记忆，先读现有记忆，再增量修改。**

#### 2. `memorySave`

文件：

`src/tools/local/memory_save.js`

作用：

- 将新的项目级记忆写入项目目录
- 将新的用户级记忆写入用户目录

输入参数有两个：

- `projectContent`
- `userContent`

它会确保目录存在，然后写到：

- `.front/memory/memory.md`
- `用户目录/.front/memory/memory.md`

这个工具让记忆更新真正落地。

所以这两个工具的关系是：

- `memoryGet` 负责读旧记忆
- `memorySave` 负责写新记忆

---

### 九、记忆工具是如何注册进系统的

文件：

`src/tools/local/index.js`

这里在原有工具基础上新增了：

```js
import memory_save from './memory_save.js';
import memory_get from './memory_get.js';
```

并注册进 `LocalClient`：

```js
localClient.registerTool(memory_save);
localClient.registerTool(memory_get);
```

这意味着：

- 记忆能力不是独立旁路逻辑
- 而是和其他本地工具一样，进入统一工具系统

也就是说，对模型来说，记忆读写也是一种标准 function tool。

---

### 十、这节课里的触发方式：手动触发和自动触发

课件专门讲了记忆触发方式的选择：

#### 1. 手动触发

由用户明确执行某个命令来触发记忆生成。

#### 2. 自动触发

由系统在满足某些条件时自动生成和更新记忆。

课件的意思是：

- 服务端型 AI 应用，可以考虑定时或后台自动触发
- 桌面型 / 终端型 AI 应用，因为没有长期常驻服务，更适合先做手动触发

这个项目当前实现采用的就是：**手动触发优先**。

---

### 十一、`/memory` 命令是怎么实现的

核心文件：

`src/commands/index.js`

这个文件新增了一个内置命令：

```js
'/memory': {
  description: '生成记忆 - 调用大模型分析上下文并写入记忆文件'
}
```

执行流程非常清晰：

#### 1. 先获取记忆模板内容

```js
const memoryContent = getMemoryContent();
```

也就是把：

- 现有记忆
- 项目上下文
- 用户上下文
- 最近对话历史

全部拼成一个完整 prompt。

#### 2. 创建 OpenAI 客户端

```js
const openai = createOpenAIClient();
```

#### 3. 构造一个只保留 `memorySave` 的工具集

```js
const memoryToolResult = {
  tools: toolResult.tools.filter(tool => tool.name === 'memorySave'),
  toolNameMap: toolResult.toolNameMap
};
```

这点很重要。它说明：

- 执行 `/memory` 时，并不想让模型随便用所有工具
- 只允许它在最后调用 `memorySave`

也就是说，记忆生成阶段的工具权限是被收窄的。

#### 4. 构造消息

```js
const memoryMessages = [{ role: 'user', content: memoryContent }];
```

这里不会再带普通聊天上下文，而是单独发一个“记忆生成专用任务”。

#### 5. 请求大模型

```js
await getAIResponse({
  openai,
  toolResult: memoryToolResult,
  contextMessageList: [],
  messages: memoryMessages,
  spinner
});
```

也就是说：

- 大模型收到模板
- 根据模板内容总结项目级和用户级记忆
- 最后调用 `memorySave`
- 由本地工具写回文件

这就是当前项目的完整手动记忆生成闭环。

---

### 十二、为什么说这是“自维护”记忆，而不是“重写”记忆

课件里非常强调这一点，而代码里也体现得很明显。

原因在于：

#### 1. 模板里显式带入了旧记忆

不是从空白开始写，而是从已有记忆继续改。

#### 2. 模板里规定了迭代原则

例如：

- 已存在内容不重复写
- 冲突时以新内容为准
- 没变化就保持原状

#### 3. 还带入了最近对话和上下文

所以最终更新逻辑更像：

> 旧记忆 + 新证据 -> 修订后的记忆

而不是：

> 新对话 -> 全量覆盖旧记忆

这就是“自维护”的核心。

---

### 十三、系统提示词里对记忆的约束

文件：

`src/docs/systemDoc.md`

这里把记忆规则继续写进了系统提示里。

主要包括两大块：

#### 1. 记忆内容应该分层

系统提示里明确写了：

- 项目级记忆只针对当前项目
- 用户级记忆针对所有项目

并且列出了应该记的六类内容：

1. 项目特点
2. 项目功能开发记录
3. 项目常见错误和 bug
4. 用户开发偏好
5. 用户技术情况
6. 用户项目情况

#### 2. 什么时候必须触发记忆

系统提示规定：

当遇见以下情况，务必通过 `memory_save` 工具写入记忆文件：

1. 用户明显表达了“希望你记住某件事”
2. 对话中出现了符合记忆点的重要内容

同时还补充了原则：

1. 先用 `memory_get` 读取当前记忆
2. 如果内容已存在，不要重复记忆
3. 如果新旧记忆冲突，以新的为准

这说明这个项目虽然当前主要实现了手动 `/memory`，但**系统提示已经在为自动触发做铺垫**。

---

### 十四、课件里为什么强调“自动触发要谨慎”

课件后面专门讲了手动触发和自动触发怎么选。

结论大致可以理解为：

#### 1. 手动触发适合桌面型/办公型 AI

因为：

- 用户就是单人使用
- token 花费由用户自己承担
- 存储成本也由用户自己承担
- 不容易被大规模滥用

#### 2. 面向大量用户的 AI Agent，要谨慎自动触发

因为自动触发会带来：

- 大量额外 token 开销
- 后端存储压力
- 恶意诱导 AI 记忆的风险

也就是说，自动记忆不是不能做，而是要看场景。

当前这个项目作为本地终端应用，先做 `/memory` 手动触发是很合理的。

---

### 十五、手动触发逻辑在课件里的完整链路

课件里对手动触发总结得很清楚，结合代码可以整理成：

1. 用户执行 `/memory`
2. 从历史记录里拿最近 50 条会话
3. 读取当前项目和用户已有记忆文件
4. 读取当前项目 `.front.md` 和用户 `.front/.front.md`
5. 套入记忆模板
6. 把组合后的内容发给大模型
7. 大模型根据规则生成项目级记忆和用户级记忆
8. 通过 `memorySave` 写回本地文件

这基本就是当前代码的真实实现路径。

---

### 十六、自动触发在这套设计里的思路

虽然当前实现主要是手动 `/memory`，但课件也给了自动触发的方向。

自动触发的思路大致是：

1. 在系统上下文中明确记忆触发条件
2. 当用户某次表达中出现“请记住”“以后都按这个来”之类强烈信号时
3. 模型先通过 `memoryGet` 拿到当前记忆
4. 再基于本次对话和上下文生成更新结果
5. 最后用 `memorySave` 写回

换句话说：

- 当前项目已经具备了自动触发的工具基础
- 只是产品层面主要还是先让用户通过 `/memory` 手动触发

这是一种比较稳的演进路线。

---

### 十七、这节课真正要记住的设计思想

#### 1. 记忆分短期和长期

- 短期记忆是当前/最近对话
- 长期记忆是用户和项目的稳定信息沉淀

#### 2. 长期记忆也要分用户级和项目级

因为“用户通用偏好”和“项目专属信息”本来就不是同一层东西。

#### 3. 记忆必须增量维护

不能每次覆盖旧记忆，否则记忆会非常不稳定。

#### 4. 生成记忆不能只看当前一句话

至少要综合：

- 当前已有记忆
- 最近对话记录
- 项目上下文
- 用户上下文

#### 5. 记忆是 Agent 自我迭代的一部分

它不是普通文件系统操作，而是让 AI 对用户越来越“熟”。

---

### 十八、当前实现里几个值得注意的点

#### 1. 记忆文件是 Markdown

这一点非常实用，便于：

- 人工检查
- 手动修正
- 调试记忆效果

#### 2. `/memory` 时只开放 `memorySave`

这是很好的权限收缩设计，避免模型在记忆生成阶段乱调其他工具。

#### 3. `memoryTemplate.md` 和 `systemDoc.md` 双层约束

这说明项目不是只靠一个模板，而是：

- 模板负责记忆任务本身
- 系统提示负责常态化记忆规则

#### 4. 历史记录取最近 50 条

这是一个很实际的折中：

- 太少，记忆更新依据不够
- 太多，prompt 变长、成本变高

所以它本质上也是一种 token 控制。

---

### 十九、小结

`6-8 记忆体系` 这一节，本质上是在给 AI 终端应用补“长期记忆能力”。

代码层面，它完成了这些事：

- 把记忆拆成项目级和用户级两层
- 用本地 Markdown 文件保存长期记忆
- 读取最近历史对话、项目上下文、用户上下文和已有记忆
- 通过 `memoryTemplate.md` 组装成记忆生成任务
- 新增 `memoryGet` / `memorySave` 两个本地工具
- 新增 `/memory` 命令手动触发记忆生成
- 通过系统提示约束未来的自动记忆行为

思想层面，它真正强调的是：

- AI 不应只靠当前几轮对话
- 长期记忆要基于旧记忆不断修正
- 记忆体系要分层、分场景、分触发方式
- 手动触发更适合当前这种本地终端型 AI 应用

所以这节课不是单纯“多加一个保存文件功能”，而是在搭建 AI Agent 的长期认知系统。





## ai应用的灵魂-工作流

这一节讲的是：**真正拉开 AI 应用差距的，不只是模型能力，也不只是工具数量，而是工作流。**

课件里的核心观点可以概括成一句话：

> 大模型像一个能力很强的实习生，真正决定它做事方式的，是我们给它规定的工作流。

也就是说：

- 模型提供推理能力
- 工具提供执行能力
- **工作流**决定“先做什么、后做什么、出错怎么回环、什么算完成”

所以这节课真正要解决的问题是：

1. 面向一个具体场景时，如何定义属于自己的 AI 工作流。
2. 这个工作流应该写到哪里，才能长期稳定生效。
3. 如何围绕工作流把输入、工具、验证、反馈串成闭环。

---

### 一、为什么说工作流才是 AI 应用的灵魂

通用 AI 工具当然也很强，但它面对的场景太广，往往只能提供“能力”，很难直接提供“你这个业务最合适的做事流程”。

而垂直 AI 应用的价值，恰恰在于把某个领域里原本由人执行的流程，沉淀成默认规则。比如前端开发场景里，真正合理的链路不是：

1. 用户提需求
2. AI 直接吐一段代码

而更应该是：

1. **接收需求，必要时接收设计图**
2. **生成或修改代码**
3. **运行预览或调试**
4. **检查报错和页面效果**
5. **如果不对，再继续修复**
6. **满足要求后再结束**

这说明 AI 应用如果只会“回答”，本质上还是聊天工具；只有把“执行、验证、回环、完成条件”都定义清楚，才算真正的工作流型应用。

---

### 二、工作流最终要写进 `system`

课件里给出的结论非常明确：**最核心的工作流规则，最终要写进 `system` 上下文。**

原因很直接：

- `system` 是整个应用里优先级最高的行为约束
- 它决定模型默认如何理解任务、何时调用工具、何时继续回环
- 它不是某一轮临时追加的说明，而是产品级常态规则

在这套项目里，对应入口就是 `src/utils/contextRead.js`。它会读取 `src/docs/systemDoc.md`，然后在 `src/app.js` 里作为系统消息注入：

```js
const systemMessage = { role: 'system', content: readSystem() };
```

所以从架构上看：

- **工作流定义层**在 `src/docs/systemDoc.md`
- **工作流执行层**在 `src/app.js + src/request/index.js + src/tools`

这也说明，工作流不是散落在某个 prompt 片段里的技巧，而是整个应用骨架的一部分。

---

### 三、这个项目当前已经具备的工作流底座

#### 1. 系统规则层：把流程写进 `systemDoc.md`

`src/docs/systemDoc.md` 里已经不只是“角色设定”，而是明确写了前端助手的执行规范，例如：

- 先扫描相关代码，再做修改
- 生成或修改文件前需要用户确认
- 写完代码后，可以进一步调试
- 如果页面能正常打开且无错误，可以继续截图
- 如果有设计图，还可以继续做设计图和截图的对比

这说明课件强调的“把工作流写进 system”在代码里已经落地了。

#### 2. 上下文装配层：把流程依赖的材料拼出来

`src/utils/contextRead.js` 负责读取和拼装系统文档、用户上下文、项目上下文、技能头信息、记忆信息等内容。

也就是说，模型真正开始工作前，程序会先把“做事规则”和“做事背景”准备好。工作流不只是顺序问题，还依赖足够完整的上下文，这一层就是在解决这个问题。

#### 3. 请求编排层：在 `app.js` 里组织一次完整请求

`src/app.js` 是整个工作流的主调度入口。它会依次处理：

1. 用户输入
2. 内置命令
3. `@[file]` 文件标签
4. 命中文件后的规则注入
5. RAG 检索结果
6. 图片输入
7. system / user / skill 等上下文

最后再统一发送给模型。

这意味着工作流并不是“用户一句话直接丢给模型”，而是程序先把这一轮任务需要的辅助信息全部编排好，再交给模型决策。

#### 4. 工具回环层：`request/index.js` 负责闭环

`src/request/index.js` 负责把可用工具交给模型，接收 `tool_calls`，执行本地工具，再把工具结果作为 `tool` 消息回喂给模型，直到模型给出最终回答。

这其实就是最典型的 Agent 闭环：

**模型决策 -> 工具执行 -> 结果回喂 -> 再次决策**

所以从底层架构来说，这个项目已经不是简单聊天程序，而是具备通用工作流引擎的 AI 终端应用。

---

### 四、当前项目里的通用工作流是怎么跑起来的

结合 `src/app.js`、`src/utils/contextRead.js`、`src/request/index.js`，可以把当前主流程概括成：

1. 用户输入需求
2. 程序先判断是否包含内置命令
3. 如果输入里带有 `@[file]`，就把文件内容附加给模型
4. 如果选中的文件命中了规则，就把匹配规则一起注入
5. 再从本地 RAG 中检索相关知识片段
6. 再补上 system、user、skill、memory 等上下文
7. 统一发给模型
8. 如果模型发起工具调用，就本地执行工具
9. 把工具执行结果回喂给模型
10. 模型继续判断是否还要下一步动作，直到完成回复

所以这个项目原本就已经具备了“输入增强 + 上下文拼装 + 工具回环”的通用工作流底座。`6-9` 这节课做的，是在这个底座上继续加前端专项流程。

---

### 五、6-9 这节课里的前端专项工作流，源码里已经开始落地

对照这份代码可以看到，课件里提到的“设计图驱动前端工作流”并不是停留在概念层，而是已经有了实际实现。

#### 1. 设计图输入已经落地

相关文件：

- `src/files/index.js`
- `src/input/index.js`
- `src/commands/index.js`

这里做了几件事：

- `src/files/index.js` 里新增了 `scanDesignImages()`，会扫描 `.front/design` 目录下的图片
- 同文件里新增了 `parseImageTags()`、`removeImageTags()`、`getDesignImagePath()`，用于解析 `#[xxx]` 图片标签并定位真实图片路径
- `src/input/index.js` 的增强输入里，除了 `/` 命令补全、`@` 文件补全，还新增了 `#` 图片选择逻辑，`type` 里明确支持 `'image'`
- `src/commands/index.js` 的帮助信息里也写明了：输入 `#` 可以显示 `.front/design` 下的图片，选中后会作为图片发送给大模型

也就是说，这一版已经把“设计图作为输入源”接进终端交互流程了，不再只有纯文本输入。

#### 2. 多模态消息拼装已经落地

相关文件：

- `src/app.js`
- `src/utils/fileHandle.js`

`src/app.js` 里已经有完整的图片处理链路：

1. 先从用户输入里解析 `#[图片名]`
2. 用 `getDesignImagePath()` 找到 `.front/design` 下的真实图片路径
3. 调用 `imageToBase64()` 把图片转成 base64 data URL
4. 把图片以

```js
{
  type: 'image_url',
  image_url: { url: dataUrl }
}
```

的形式追加进本轮 `messages`

5. 同时把图片绝对路径作为文本补充进消息里，便于后续工具链继续使用

这一步非常关键，因为它意味着“设计图 -> 多模态请求”这条链已经被真正接通了。

#### 3. 调试预览工作流已经落地

相关文件：

- `src/tools/local/debugger_page.js`
- `src/utils/debuggerUtils.js`
- `src/docs/systemDoc.md`

`src/docs/systemDoc.md` 里已经明确写了：

- 代码写入后，可以确认是否需要调试
- 如果需要，就调用 `debugger_page`
- 工具需要传入项目的页面地址
- 如果页面报错，返回错误信息
- 如果页面没报错，返回截图地址

对应实现上：

- `debugger_page.js` 暴露了 `debugger_page` 工具
- 这个工具会先调用 `launchBrowserForCapture(url)` 启动带远程调试端口的 Chromium
- 再调用 `captureRunningBrowser()` 连接浏览器并监听页面

`captureRunningBrowser()` 的核心逻辑也很清楚：

1. 通过 `chromium.connectOverCDP()` 连接浏览器
2. 获取目标页面
3. 收集 `console` 日志和 `pageerror`
4. 如果有错误，直接返回错误结果
5. 如果没有错误，就把截图保存到 `.front/screenshot`
6. 返回 `success`、`errors`、`logs`、`screenshotPath`、`pageUrl` 等结构化结果

这说明“写代码 -> 打开页面 -> 看控制台 -> 截图”的验证环节已经被纳入工作流。

#### 4. 设计图和截图对比工作流也已经落地

相关文件：

- `src/tools/local/diff_pic.js`
- `src/docs/systemDoc.md`

`systemDoc.md` 里已经继续规定：如果拿到了截图，可以询问用户是否需要调用 `diff_pic` 去对比设计图和测试截图。

`diff_pic.js` 的实现也比较直接：

1. 接收 `design` 和 `screenshot` 两个绝对路径
2. 把两张图都转成 base64
3. 以多模态消息的形式发给模型
4. 让模型输出“设计图和截图的具体差异”
5. 再把这段差异描述返回给主流程

也就是说，这里已经形成了：

**设计图 -> 生成代码 -> 页面预览 -> 截图 -> 差异分析 -> 再修复**

这样的前端专项工作流闭环雏形。

---

### 六、这节课真正强调的是“闭环”

从课件到代码，最关键的思想都不是“多接一个工具”，而是把流程做成闭环。

一个完整的前端 AI 工作流至少应该包含：

1. 输入阶段
   用户输入需求，必要时再输入设计图。
2. 执行阶段
   AI 生成代码、修改代码、调用工具。
3. 验证阶段
   自动检查页面是否报错，是否能正常加载。
4. 反馈阶段
   如果有报错就把错误回喂给模型；如果没报错就继续截图和比图。
5. 完成阶段
   直到页面通过验证，或者用户确认效果满足要求，流程才结束。

所以这节课里“工作流是灵魂”的真正含义是：**让 AI 不只是会生成，还会验证、会对比、会继续修复。**

---

### 七、为什么通用 AI 工具不容易替你定义这类工作流

课件里拿 `claude code` 这类通用工具做对比，重点并不是说它不强，而是说它太通用，所以很多工作流只能靠用户临时描述，或者靠用户自己在 skill 里补。

而你如果是给某个固定场景做专门的 AI 应用，就可以把这些内容提前内建进去：

- 这个领域该怎么接收输入
- 什么时候需要调用哪些工具
- 怎样验证结果
- 出错以后按什么顺序回环
- 什么条件下才算完成

这就是垂直 AI 应用真正的差异化来源。它不一定模型更强，但它更懂“这件事应该怎么做”。

---

### 八、结合这份代码，如何理解 `6-9`

结合课件和源码，这一节可以这样理解：

#### 1. 项目原本就已经有通用工作流引擎

体现在：

- `systemDoc.md` 负责定义全局规则
- `app.js` 负责编排上下文和输入
- `request/index.js` 负责工具回环
- 命令系统和本地工具系统负责承载具体动作

#### 2. `6-9` 开始把通用引擎推进成前端专项工作流

体现在：

- 用 `.front/design` 和 `#` 机制接入设计图
- 用 `image_url` 形式把设计图发给模型
- 用 `debugger_page` 做页面预览和控制台检查
- 用 `.front/screenshot` 保存页面截图
- 用 `diff_pic` 分析设计图和截图差异

所以这节课不是停留在“讲理念”，而是在演示如何把一个通用 AI Agent，逐步演进成“懂前端交付流程”的专业应用。

---

### 九、小结

`6-9 AI应用的灵魂-工作流` 这一节，本质上是在讲：**AI 应用的竞争力，不只是模型和工具，而是你有没有把行业里的最佳做事流程沉淀进系统。**

结合这份源码，重点可以总结成三点：

1. 工作流要沉淀进 `system`，成为应用级默认行为规范。
2. 工具不是孤立堆砌，而是要围绕“输入 -> 执行 -> 验证 -> 回环”来设计。
3. `6-9` 这份代码已经把前端专项工作流做出了雏形，尤其是设计图输入、页面调试截图、设计图对比修复这条链路，已经开始真实落地。

所以这节课真正教的，不是“再多接几个工具”，而是：**怎么把一个会聊天、会调用工具的大模型，变成一个会按前端交付流程做事的 AI 应用。**
