# ai项目继续实现

## 核心工具实现（6-6)

这一节的重点不是单独学几个工具文件怎么写，而是理解两件事：

1. 为什么一个 AI 终端应用，只要接上合适的工具，就能一下子从“只会聊天”变成“能直接做事”。
2. 一个工具要怎样设计，才能让大模型稳定地在正确场景下调用它。

课件里反复强调一句话：`AI 应用的大部分执行能力，本质上都是工具提供的。`

像 Claude Code、Codex 这类终端型 AI 助手，看起来像“什么都会”，本质上并不是模型本身会操作电脑，而是它背后接了很多工具，比如：

- 读文件
- 写文件
- 全局搜索代码
- 按文件名查找文件
- 执行终端命令
- 向用户发起确认
- 让用户做选项选择

所以这节课的“核心工具开发”，本质是在给模型补手、补眼睛、补执行器。

---

### 一、这套工具层解决的核心问题

如果没有工具，大模型只能根据你贴给它的上下文做文本回复。

一旦接入工具，它就可以：

- 先搜索项目里有哪些文件
- 再定位某个函数写在哪
- 再读取对应文件内容
- 再生成修改后的代码
- 再写回文件
- 必要时执行命令验证
- 遇到危险操作前先征求用户确认

这就是为什么“工具”会决定 AI 应用的上限。

从课件给的例子看，桌面/终端类 AI 应用不只是“写代码”，还要能帮用户做真实操作，例如：

- 查询本机 IP
- 排查某个地址为什么请求不通
- 安装 npm 包
- 执行 git 提交
- 扫描和处理本地文件

这些都不是纯文本能力，而是工具能力。

---

### 二、本地工具层的整体结构

参考代码目录：`C:\Users\MJL\Desktop\ai学习\ppt和源码\6-6 核心工具开发\code\src\tools`

其中主要分成两块：

- `src/tools/local`
  这一层是本地自定义工具，直接在当前项目里实现。
- `src/tools/mcp`
  这一层负责连接外部 MCP 服务，把 MCP 工具也并入统一的工具列表。

最终目标是：**无论工具来源于本地还是 MCP，都统一成 OpenAI function calling 需要的格式，交给模型调用。**

整体调用链可以概括成：

1. 在 `src/tools/local/index.js` 中注册本地工具。
2. 在 `src/tools/mcp/index.js` 中连接外部 MCP 服务并拿到它们的工具列表。
3. 在 `src/tools/index.js` 中把两类工具合并，形成统一的 `tools` 和 `toolNameMap`。
4. 在 `src/tools/util.js` 中把工具描述转换成 OpenAI 需要的 `type: "function"` 格式。
5. 在 `src/request/index.js` 里把这些工具随请求一并发给模型。
6. 当模型返回 `tool_calls` 时，再由本地代码根据工具名执行真实逻辑。

所以这一层其实就是一个“工具调度中枢”。

---

### 三、本地中比较重要的工具

#### 1. `read_file`

文件：`src/tools/local/read_file.js`

作用：

- 读取指定本地文件内容
- 支持按行偏移 `offset`
- 支持限制读取行数 `limit`

主要实现点：

- 使用 `path.resolve(file_path)` 转成绝对路径
- 先校验文件是否存在
- 校验路径是否是文件
- 限制文件大小不超过 `1MB`
- 读取后按换行拆分
- 输出时带行号，方便模型引用和定位

这类工具非常重要，因为模型修改代码前，通常必须先看原始内容。

课件里也强调：**读取文件应优先使用专用工具，而不是让模型自己猜文件内容。**

这个实现里比较好的细节有：

- 限制大文件，避免一次塞太多上下文
- 行号输出，方便精准修改
- 支持局部读取，避免上下文浪费

#### 2. `write_file`

文件：`src/tools/local/write_file.js`

作用：

- 将内容写入指定文件
- 如果目录不存在则自动创建
- 如果文件已存在则覆盖

主要实现点：

- `path.resolve(file_path)` 统一路径
- `path.dirname(resolvedPath)` 获取父目录
- 不存在则 `fs.mkdirSync(dir, { recursive: true })`
- 用 `fs.writeFileSync` 写入

这个工具在 AI 终端里属于“执行落地”的关键一步。没有它，模型最多只能给出建议；有了它，模型才真正能修改项目。

当前示例里它在成功后会把“写入内容”也返回出来，这样模型可以立刻对自己刚生成的代码做二次反思，比如：

- 有没有明显 bug
- 有没有冗余代码
- 有没有更优方案

#### 3. `bash`

文件：`src/tools/local/bash.js`

作用：

- 在无法通过其他工具完成任务时，执行终端命令

它的定位不是“默认工具”，而是“兜底工具”。

课件里的场景包括：

- 查询 IP
- `ping` 检查网络
- `tracert` / `traceroute` 排查链路
- 安装 npm 包
- 执行 git 操作

实现要点：

- 使用 `child_process.exec`
- 配合 `promisify` 转成异步调用
- 用 `os.platform()` 判断操作系统
- Windows 下改成通过 PowerShell 执行

代码里的兼容逻辑：

```js
if (platform === 'windows') {
  finalCommand = `chcp 65001 >nul && powershell -Command "${command}"`;
}
```

这段处理是为了：

- 保证 Windows 终端执行可用
- 切换到 UTF-8 编码，减少中文乱码

这里也能看出课件的一个思想：**工具不是只要能跑就行，还要考虑跨平台和用户环境。**

#### 4. `grep`

文件：`src/tools/local/grep.js`

作用：

- 在项目中全局搜索代码内容
- 支持正则或普通字符串
- 支持 `glob` 过滤文件类型
- 支持两种输出模式

课件对搜索工具强调得很多，因为模型在真实项目中最常见的第一步不是直接改代码，而是先找代码。

支持参数：

- `pattern`：搜索关键字或正则
- `path`：搜索根目录
- `glob`：文件过滤规则
- `output_mode`：`files_with_matches` 或 `content`

实现上的关键点：

- 使用 `walkDir` 递归遍历目录
- 跳过 `node_modules`、`.git`、`dist`、`build` 等目录
- 跳过二进制文件
- 跳过大于 `1MB` 的文件
- 限制最多扫描 `500` 个文件
- 限制最多返回 `100` 个匹配结果

这说明工具设计不只是“功能要有”，还要注意：

- 性能
- 上下文大小
- 噪音控制
- 搜索精度

另外，示例描述中明确要求：

> 只要是搜索相关任务，就优先用 `grep`，不要让模型自己去拼 bash 搜索命令。

这个约束很重要，因为如果不在描述里强调，模型很可能会：

- 乱用 `bash`
- 自己拼 `grep` / `findstr` / `rg`
- 造成跨平台问题

所以工具描述本身就是“行为引导”。

#### 5. `glob`

文件：`src/tools/local/glob.js`

作用：

- 按文件名模式查找文件
- 返回匹配到的文件路径列表

典型场景：

- 看项目结构
- 找某一类文件
- 根据文件名规则筛选候选文件

例如：

- `*.js`
- `src/**/*.js`
- `**/*.md`

它和 `grep` 的区别是：

- `glob` 搜的是“文件名/路径模式”
- `grep` 搜的是“文件内容”

课件里强调，AI 要先知道“有哪些文件”，再决定“读哪些文件”。所以 `glob` 和 `grep` 常常是搭配使用的。

实现点包括：

- 同样跳过无关目录
- 使用 `minimatch` 做 glob 匹配
- 限制最大返回结果数 `1000`

#### 6. `confirm`

文件：`src/tools/local/confirm.js`

作用：

- 在终端中向用户发起 yes/no 确认

使用了：

```js
import { confirm } from '@inquirer/prompts';
```

典型场景：

- 写文件前确认
- 删除/覆盖操作前确认
- 可能带来风险的操作前确认

课件里强调过：当 AI 需要做危险操作、不可逆操作、可能造成数据泄露的操作时，必须先和用户确认。

所以 `confirm` 工具不是锦上添花，而是安全边界的一部分。

#### 7. `select`

文件：`src/tools/local/select.js`

作用：

- 当用户需求不明确时，向用户展示候选项，让用户做选择

使用了：

```js
import { select } from '@inquirer/prompts';
```

典型场景：

- 需求存在多种解释
- 存在多个技术方案
- 某些关键细节无法靠推断确定

比如：

- 让用户选择改哪个文件
- 选择实现方案
- 选择环境配置项

这类工具可以减少模型“自作主张”的概率。

#### 8. `skill`

文件：`src/tools/local/skill.js`

作用：

- 读取某个 skill 文件的内容

它的实现很简单，本质就是把指定路径的 skill 文本加载出来返回。

在整个项目里，skill 更偏向“给模型补额外规则/能力说明”，所以这个工具更像是上下文装载器。

---

### 四、“工具描述”

重点不在实现，而在“怎么写一个让模型会用、敢用、少用错的工具描述”。

老师强调：

- 工具开发不是把功能写完就结束
- 更重要的是大量测试，然后不断修改**工具描述和上下文**
- 最终目标不是 100% 完美，而是至少在 90% 该调用的场景里，模型真的能调用对

也就是说，一个工具的质量，至少包含两层：

1. 功能代码本身正确
2. 模型能在正确时机想到它、调用它、传对参数

如果只写代码，不调描述，最后的效果通常会很差。

---

### 五、工具描述一般应该包含什么

根据课件总结，一个成熟的工具描述通常包含下面几部分：

#### 1. 基本介绍和工作原理

要让模型知道：

- 这个工具是干什么的
- 它和其他工具有什么区别
- 它大概如何工作

#### 2. 使用情境

这是最重要的部分之一。必须明确告诉模型：

- 什么场景该用这个工具
- 什么场景不要用
- 和其他工具冲突时优先谁

比如这节课中的典型写法：

- 搜索内容优先用 `grep`
- 按文件名找文件优先用 `glob`
- 其他工具搞不定时再考虑 `bash`

#### 3. 使用规范

如果实际测试发现模型经常误用，就要继续补规则。

例如：

- 不要自己拼 shell 搜索命令
- 读取文件优先用 `read_file`
- 危险操作前必须 `confirm`

这部分是动态长出来的，不是一次写完的。

#### 4. 输入输出说明

让模型清楚：

- 需要哪些参数
- 参数格式是什么
- 返回结果大概长什么样

这样能显著减少参数传错的概率。

### 案例

- 案例一

```js
description: "在终端向用户发起一个确认提问，等待用户输入 yes 或 no，返回用户的确认结果",
```

- 案例二

```js
description: "当需要做一些不能通过其他工具完成的事情，可以借助该工具，通过给入Bash指令完成操作，如果用户是window系统我们会使用powerShell执行执行，如果是其他则直接执行"
```



---

---

### 六、从 `systemDoc.md` 也能看出工具描述思路

参考文件：`src/docs/systemDoc.md`

这个系统提示词里其实已经把工具使用规范写得很明确，例如：

- 阅读文件优先 `read_file`
- 搜索内容优先 `grep`
- 查文件优先 `glob`
- 向用户确认优先 `confirm`
- 需求不明确时可用 `select`
- 生成代码后要用 `write_file` 写回

这说明项目不是只靠工具定义本身，还通过系统提示继续加强模型的使用习惯。

也就是说，**真正的工具效果 = 工具代码 + 工具描述 + 系统提示里的使用规范**。

### 案例

- 工具描述

  ```js
  define: {
          name: "grep",
          description: "在当前项目中全局搜索文件内容，基于 Node.js 实现，跨平台兼容（支持 Windows / macOS / Linux）。支持正则或字符串匹配，可按 glob 过滤文件类型，自动跳过 node_modules、.git、二进制文件等。本工具只用于搜索相关代码，不用其他搜索。如果要进行搜索，一定要使用此工具，不要自行用bash命令解决",
          inputSchema: {
  ```

- 系统提示词

  ```js
  # 任务执行规范
  .....
  7. 如果用户的需求，有多种可能，比如有多种技术可选，或者有一些比较重要的细节没确定，可以使用select工具想起提供选项确认
  <!-- 6. 当生成代码，和用户确认是否需要调试，调试优先使用内置的调试工具。除非用户明确拒绝帮助调试，或者不希望调试。则不进行 -->
  
  # 内部工具使用规范
  1. 每次当你给用户生成了代码，一定要使用write_file工具把代码写入，除非用户明确要求不要写入。
  2. 如果需要阅读某个文件，获取到该文件目前内容作为参考。优先使用read_file工具
  3. 搜索相关代码使用grep工具，查询指定文件使用glob工具
  4. 如果需要向用户获取许可或者确认，或者询问用户是否需要写入文件，使用confirm工具
  5. 除非用户明确说明，否则生成文件和危险操作前一定要和用户通过confirm工具确认
  ```

---

### 七、这节代码里值得记住的设计思想

#### 1. 本地工具和 MCP 工具统一接口

通过 `LocalClient` 模拟 MCP 调用方式，这是很实用的抽象。

#### 2. 工具分成“定义”和“执行”两层

- `define` 给模型看
- `handle` 给程序执行

这让结构很清晰。

#### 3. 工具能力决定 AI 上限

没有工具，模型只能聊天；有工具，模型才能真正操作项目和终端。

#### 4. 搜索类工具要细分

不能只靠一个 `bash` 包打天下，应该拆成：

- `glob` 查文件
- `grep` 查内容
- `read_file` 看细节

这样模型的行为会更稳定。

#### 5. 工具描述和使用规范必须持续迭代

工具不好用，很多时候不是代码错了，而是描述不够清楚，模型不知道什么时候该用。

---

### 八、这一节核心结论

#### 工具就是ai大模型的手，openclaw之所以这么强大因为其内部工具很多

#### 但工具再多，也要遵循这一节的核心原则：

- 接口统一
- 描述清晰
- 场景明确
- 尽量避免让模型走“万能 bash”
- 通过测试不断补充约束





## 知识库RAG体系

这一节讲的是：**如何让 AI 终端应用读取用户自己的本地资料，并在提问时自动检索相关内容，再把这些内容补充进上下文。**

如果说上一节“核心工具”是在给模型补手，那么这一节的 RAG，本质上是在给模型补“外部知识记忆”。

课件里的核心目标非常明确：

- 允许用户把自己的资料放到本地目录里
- 将这些资料向量化后存进本地向量库
- 用户提问时，把问题也转成向量
- 在本地向量库里检索最相关的片段
- 再把这些片段按照模板拼进请求上下文，交给大模型回答

所以这一节不是泛泛讲 RAG 理论，而是在当前项目里落地一个**本地知识库增强方案**。

---

### 一、这一节要解决什么问题

没有 RAG 时，大模型只能依赖：

- 预训练知识
- 当前这轮会话上下文
- 用户临时粘贴给它的资料

这会带来几个问题：

1. 用户本地有很多资料，但不可能每次都手动粘贴。
2. 资料很多时，整份塞进 prompt 会非常浪费 token。
3. 某些知识只属于当前用户或当前项目，大模型原本并不知道。

所以需要一层本地知识库能力，让用户的私有资料也能参与回答。

课件给出的做法是：

- 用户把资料放进 `.front/doc`
- 通过 `/vector` 指令先做向量化入库
- 每次提问时自动检索相关片段
- 再把相关片段补到 prompt 中

这就是项目里的 RAG 体系。

---

### 二、这个项目里的 RAG 总流程

结合课件和代码，整体流程可以拆成两阶段：

#### 1. 建库阶段

把本地文档处理成向量并存入 LanceDB。

流程是：

1. 扫描文档目录
2. 读取文档内容
3. 文本切片
4. 每个片段调用 embedding 模型转向量
5. 把向量、原文片段、来源路径一起存入本地向量库

#### 2. 检索阶段

用户提问时，先查向量库，再把结果拼回上下文。

流程是：

1. 把用户问题转成向量
2. 在 user 级和当前项目级的 LanceDB 里相似度搜索
3. 拿到最相关的若干文本片段
4. 套用 `ragTemplate.md`
5. 作为额外上下文发给大模型

这就是一个完整的“检索增强生成”闭环。

---

### 三、文档从哪里来

课件里定义得很明确：用户可以在 `.front/doc` 目录下放自己的资料。

而且这里分成两类来源：

- 用户全局目录下的 `.front/doc`
- 当前项目目录下的 `.front/doc`

这样做的好处是：

- **全局文档**适合放通用资料，比如个人工作规范、常见制度、公司流程。
- **项目文档**适合放只对当前项目有效的资料，比如接口说明、业务规则、产品文档。

对应代码在 `src/utils/ragHandle.js` 的 `getAllRagFile()`：

- `userDocDir = path.join(userHomeDir, '.front', 'doc')`
- `currentDirDocDir = path.join(currentWorkingDir, '.front', 'doc')`

然后分别读取两边的文件，再返回：

```js
{
  userDocArr,
  currentDirDocArr
}
```

所以这套 RAG 体系本质上也是“双层作用域”：

- 用户级知识库
- 项目级知识库

这和前面 MCP 配置、command、skill 的“用户级 + 项目级”设计是一致的。

---

### 四、支持哪些文档格式

在 `ragHandle.js` 里，允许进入 RAG 流程的文件类型是：

```js
const ALLOWED_EXTENSIONS = ['.md', '.txt', '.docx'];
```

也就是说当前只支持：

- `md`
- `txt`
- `docx`

原因很现实：

- 这几类文件比较适合抽纯文本
- 文档结构相对简单
- 适合直接切片做 embedding

对应方法是 `getFilesFromDir(dir)`，它会：

1. 读取目录下文件名
2. 按扩展名过滤
3. 拼出完整路径数组

这一步本质上就是“准备候选文档列表”。

---

### 五、文档内容如何读取

核心方法：`readFileContent(filePaths)`

这个方法支持：

- 单文件路径
- 文件路径数组

最终统一返回：

```js
[
  { path: '...', content: '...' }
]
```

实现逻辑分两种：

#### 1. 普通文本类文件

对于 `.md`、`.txt`：

```js
content = fs.readFileSync(filePath, 'utf-8');
```

#### 2. Word 文档

对于 `.docx`：

```js
const result = await mammoth.extractRawText({ path: filePath });
content = result.value;
```

这里用了 `mammoth` 来提取 Word 文档纯文本。

这一层设计说明：RAG 的第一步不是“向量化”，而是**先把各种本地文档统一转换成纯文本内容**。

---

### 六、为什么要先切片再做向量化

课件里专门讲了“向量化本地文档”的流程，其中一个关键点就是：**不是整篇文档直接转一个向量，而是先切片。**

对应代码在 `filesToEmbedding(fileArr)`：

```js
const splitter = new RecursiveCharacterTextSplitter({
  chunkSize: 40,
  chunkOverlap: 20,
});
```

这里使用的是 LangChain 的 `RecursiveCharacterTextSplitter`。

参数含义：

- `chunkSize: 40`
  每个文本块大约 40 个字符
- `chunkOverlap: 20`
  相邻文本块重叠 20 个字符

为什么要切片：

1. 文档太长，整篇向量会过于粗糙。
2. 用户提问通常只和文档中的某个局部片段相关。
3. 切片后检索会更精准。
4. 只取相关片段加入上下文，更省 token。

重叠的意义也很重要：

- 防止关键信息刚好落在切片边界处被截断
- 保留一定上下文连续性

所以 RAG 的关键不是“有没有向量库”，而是**切片策略是否合理**。

---

### 七、这个项目是怎么做 embedding 的

核心方法：`getEmbeddings(text)`

实现很直接：

```js
const response = await openai.embeddings.create({
  model: 'text-embedding-v4',
  input: text,
});
return response.data[0].embedding;
```

这里的逻辑是：

1. 创建 OpenAI 客户端
2. 调用 embedding 接口
3. 拿到当前文本片段对应的向量

然后在 `filesToEmbedding()` 中，对所有切片并发执行 embedding：

```js
const embeddings = await Promise.all(
  allChunks.map(chunk => getEmbeddings(chunk.text))
);
```

最后形成统一结构：

```js
{
  vector,
  text,
  path
}
```

也就是每一条入库数据至少包含三部分：

- `vector`：向量
- `text`：原始片段文本
- `path`：来源文件路径

这点很重要，因为检索阶段真正喂给模型的，不是向量本身，而是 `text`。

---

### 八、为什么选 LanceDB 做本地向量库

课件里使用的是 `LanceDB`，对应依赖：

```js
import * as lancedb from '@lancedb/lancedb';
```

核心原因可以理解为：

- 它适合本地使用
- 不需要额外部署远程数据库
- 适合桌面/终端类 AI 应用

对于这个项目来说，RAG 的诉求不是“企业级分布式检索”，而是：

- 能在本地快速存储向量
- 能在本地快速相似度搜索
- 接入简单

LanceDB 正好符合这个场景。

---

### 九、向量数据存在哪里

核心方法：`storeIn(type, vectors)`

它会根据 `type` 决定存储位置：

#### 1. 用户级知识库

```js
dbPath = path.join(getUserHomeDir(), '.front', 'langcedb-data');
```

#### 2. 项目级知识库

```js
dbPath = path.join(getCurrentWorkingDir(), '.front', 'langcedb-data');
```

然后统一连接 LanceDB：

```js
const db = await lancedb.connect(dbPath);
```

表名固定使用：

```js
const tableName = 'doc-table';
```

如果表已存在，就追加：

```js
await table.add(vectors, { mode: 'append' });
```

- vectors上面有

如果不存在，就创建：

```js
await db.createTable(tableName, vectors);
```

这里可以看出当前实现的思路很直接：

- user 文档和 project 文档各自存一份库
- 每份库里都用同一个表名 `doc-table`

这样查询时也容易统一处理。

---

### 十、批量建库和单文件增量建库

课件里专门讲了一个优化点：**不要每次都把所有文档全部重新向量化**，那会很浪费 token。

这部分在代码里体现成两种入口：

#### 1. 全量建库：`storeAllFilesIn()`

作用：

- 扫描所有 user/project 文档
- 全部读取
- 全部切片
- 全部 embedding
- 全部入库

适合第一次初始化知识库。

#### 2. 单文件建库：`searchFileAndStoreIn(fileName)`

作用：

- 在 user 的 `.front/doc` 和当前项目 `.front/doc` 里查指定文件
- 找到后只处理这个文件
- 再把结果追加进向量库

课件强调的 token 优化点就在这里：

如果只是新增了一个文档，没必要把整个知识库重建一遍。

所以这个方法本质上是一个“增量入库”入口。

---

### 十一、`/vector` 指令是如何接入的

核心文件：`src/commands/index.js`

这里新增了内置命令：

```js
'/vector': {
  description: '将所有本地文档向量化并存入数据库，或指定单个文件 /vector <file>',
}
```

执行逻辑是：

#### 1. 不带参数

```js
await storeAllFilesIn();
```

表示全量向量化所有文档。

#### 2. 带文件名参数

```js
await searchFileAndStoreIn(args);
```

表示只向量化指定文件。

这正好对应课件里讲的两类使用方式：

- `/vector`
- `/vector 文件名`

所以从交互角度看，RAG 的建库并不是偷偷后台完成，而是通过命令显式触发。

---

### 十二、用户提问时是怎么触发检索的

核心接入点在：`src/app.js`

在用户输入之后、真正发请求给模型之前，代码会先执行：

```js
const ragTexts = await searchLocalVector(trimmedInput);
```

也就是说：

- 用户每次提问
- 系统都会尝试拿这句话去本地向量库搜一遍

这就是“检索增强”的入口。

---

### 十三、检索过程具体怎么做

核心方法：`searchLocalVector(queryText)`

流程如下：

#### 1. 把问题转成向量

```js
const vector = await getEmbeddings(queryText);
```

#### 2. 分别搜索两份本地库

- 用户目录下的 `langcedb-data`
- 当前项目目录下的 `langcedb-data`

#### 3. 如果存在 `doc-table`，就做相似度搜索

```js
userResults = await table.search(vector).limit(3).toArray();
currentResults = await table.search(vector).limit(3).toArray();
```

这里每个库最多取 `3` 条。

#### 4. 合并结果并返回文本

```js
return [...userResults, ...currentResults].map(r => r.text);
```

所以这一层返回的并不是完整数据库记录，而是命中的文本片段数组。

可以理解成：

- 输入：用户问题
- 输出：和问题最相关的若干知识片段

---

### 十四、检索结果是怎么注入 prompt 的

检索到片段后，`app.js` 中会继续做一层模板封装：

```js
const ragTemplatePath = path.join(__dirname, './docs/ragTemplate.md');
let ragTemplate = fs.readFileSync(ragTemplatePath, 'utf-8');
ragMessage.content = ragTemplate.replace('${ragContent}', ragContent)
```

而 `ragTemplate.md` 的内容很简单：

```md
用户有如下参考资料提供到
${ragContent}
```

也就是说，这个项目不是直接把检索结果裸塞给模型，而是先包一层“提示模板”。

最终得到一个额外消息：

```js
let ragMessage = { role: "user", content: "..." };
```

然后把它和：

- `systemMessage`
- `userContextMessage`
- `userSkillMessage`

一起放进 `contextMessageList`：

```js
contextMessageList: [systemMessage, userContextMessage, userSkillMessage, ragMessage]
```

这就意味着：

- RAG 结果不是作为工具返回
- 也不是作为主问题正文的一部分
- 而是作为“额外上下文消息”单独注入给模型

这种组织方式比较清晰，也便于后续继续扩展模板。

---

### 十五、这套 RAG 体系的一个核心价值：省 token

课件里单独强调了 token 优化问题。

如果用户本地有很多资料，但每次提问都把所有资料全文塞进 prompt，会有几个问题：

- token 消耗巨大
- 响应慢
- 噪音多
- 模型注意力容易分散

RAG 的优势就在于：

- 先把文档离线向量化
- 提问时只检索最相关片段
- 实际注入模型的只是少量命中内容

所以它不是简单的“多一份知识”，而是一个**更经济的上下文供应机制**。

---

### 十六、课件里对 RAG 和 Skill 的对比很重要

这一节后半段其实不是只讲 RAG，而是在讲：**什么时候该用 RAG，什么时候该用 Skill。**

这是最容易混淆、但也最值得记住的部分。

#### 1. Skill 的特点

Skill 不只是提供资料，还可以指导 AI 怎么做事。

也就是说 Skill 更像：

- 规则
- 工作指引
- 行为规范
- 任务流程说明

它不只是“给内容”，还会“教模型怎么干活”。

#### 2. RAG 的特点

RAG 更偏向：

- 提供参考资料
- 从大量资料里检索相关片段

它擅长的是“找内容”，而不是“规定行为”。

课件里一句话概括得很到位：

- `skill`：除了提供资料，还可以指导 AI 怎么干活
- `rag`：只能提供资料

这就是两者最本质的区别。

---

### 十七、为什么说 RAG 在检索速度上通常优于 Skill

课件里还从速度上做了对比。

#### 1. Skill 的问题

Skill 往往需要：

- 让模型先判断要不要加载
- 再去加载 skill
- 再结合 skill 回答

这会带来：

- 额外判断成本
- 额外上下文组织成本
- 有时还要多轮思考

#### 2. RAG 的优势

RAG 的流程更直接：

- 问题转向量
- 本地检索
- 拼装结果
- 一次发给模型

本地向量搜索本身非常快，所以适合高频问答场景。

课件的意思可以总结成一句话：

**Skill 更像显式加载规则包，RAG 更像高速局部检索资料库。**

---

### 十八、什么时候适合用 RAG

根据课件内容，可以总结出几种典型场景：

#### 1. 资料很多

如果你有大量文档资料，需要从中精确命中局部片段，适合用 RAG。

因为 RAG 会：

- 先切片
- 再检索
- 只取相关部分

更省 token。

#### 2. 这些资料使用频率很高

课件明确提到：

如果某些本地资料几乎每次提问都要参考，RAG 会更合适。

原因是：

- 检索速度快
- 不依赖模型先判断是否加载 skill

#### 3. 资料本质上是“参考信息”，而不是操作规则

例如：

- 制度文档
- 说明文档
- 业务资料
- 常见问答文档

这种内容更适合被切片检索。

---

### 十九、什么时候不适合只用 RAG，而更适合 Skill

课件也给了很明确的边界：

#### 1. 你需要给模型完整的工作指引

如果你的目标不只是“提供资料”，而是：

- 规定模型怎么分析
- 规定模型怎么执行
- 规定模型遵循什么流程

那应该优先用 Skill。

#### 2. 资料内容不多，但必须完整加载

RAG 的天然问题是会切片。

一旦切片，就很难保证模型看到的是“完整原文”。

所以如果资料：

- 很短
- 很精炼
- 一行都不能丢
- 必须完整交给模型

那更适合用 Skill，而不是 RAG。

#### 3. 资料既是内容，也是规则

比如某些规范文档，不只是“参考知识”，而是“必须执行的行为约束”。

这种内容用 Skill 更稳，因为 Skill 是显式上下文，不依赖检索命中概率。

---

### 二十、当前实现里值得注意的几个细节

#### 1. user 和 project 两套库分开存

这是很好的层次划分：

- 用户长期知识和项目临时知识不会混在一起
- 同一个问题可以同时检索两边

#### 2. 单文件增量向量化能节省成本

`/vector 文件名` 这个设计很实用，避免了每次全量重建。

#### 3. 入库内容同时保留 `text` 和 `path`

这说明后续如果需要：

- 展示来源文件
- 做结果溯源
- 去重

都还有扩展空间。

#### 4. RAG 注入使用模板而不是硬拼字符串

虽然当前 `ragTemplate.md` 很简单，但这种写法为后续扩展留了位置，比如：

- 增加来源说明
- 增加引用格式
- 增加“仅供参考”的提示语

---

### 二十一、这一节的设计思想

#### 1. RAG 是“资料增强”，不是“能力增强”

它能补充模型不知道的内容，但不会直接教模型如何执行复杂流程。

#### 2. RAG 更像本地检索系统

它的价值在于：

- 面向大量资料
- 做局部精准召回
- 控制上下文成本

#### 3. Skill 和 RAG 是互补关系，不是替代关系

可以这样记：

- `Skill` 负责“怎么做”
- `RAG` 负责“有哪些资料可参考”

很多场景里两者应该一起用。

#### 4. RAG 在 AI 终端里特别适合“用户私有知识”

因为这些内容：

- 大模型原本不知道
- 又不能每次手动复制
- 还经常需要引用

---

### 二十二、小结

`6-7 RAG体系` 这一节，本质上是在当前项目里补了一套本地知识库系统。

代码层面，它完成了这些事：

- 扫描 `.front/doc` 文档
- 读取 `md`、`txt`、`docx`
- 切片文本
- 调用 embedding 模型生成向量
- 存入本地 LanceDB
- 提供 `/vector` 命令进行全量或增量建库
- 在用户提问时自动检索相关片段
- 通过 `ragTemplate.md` 注入额外上下文

思想层面，它真正强调的是：

- RAG 适合大量资料的检索增强
- RAG 的核心价值是“精准召回 + 节省 token”
- RAG 只负责提供资料，不负责教模型怎么做事
- Skill 更适合规则、流程、工作方法这类内容

所以这节课不是单纯“接一个向量库”，而是在给 AI 终端应用补齐“本地私有知识检索能力”。
