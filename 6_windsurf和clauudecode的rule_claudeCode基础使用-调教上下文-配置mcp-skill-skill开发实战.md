# 问题：windsurf是否有md可以自定义呢

**Rules = 行为规范（必须遵守）**

**Memories = 长期笔记（参考用）**

**MCP = 外部数据源 / 工具的 “标准接口”（让 AI 能联网、连数据库、用 API）**

### Claude Code：`.claude/` 下面的 md

- 核心：**`CLAUDE.md`（项目根 或 `.claude/` 里）**
- 扩展：`.claude/rules/`、`.claude/skills/` 等目录，可拆成多个 md
- 本质：**项目级的系统提示词 + 长期记忆 + 行为约束**
- 加载：**每次会话自动读，直接打进上下文**

### Windsurf：`.windsurf/rules/` 或 `.windsurfrules`

- 核心：**`.windsurfrules`（项目根）**
- 扩展：新版支持 **`.windsurf/rules/\*.md`** 多文件
- 本质：**给 Cascade 的全局行为规则 / 编码规范 / 项目上下文**
- 加载：**每次对话自动注入，影响所有生成**

## 2. 作用是不是一样？

### 相同点（80% 一致）

- 都是**持久化指令**，不用每次重复说明项目情况
- 都写：技术栈、代码风格、命名规范、目录结构、禁止行为
- 都相当于给 AI **“项目入职手册”**
- 都属于 **AI Agent 的配置层（上下文 + 规则）**，不是模型 weights

### 细微差别（20% 不一样）

- **Claude（.claude/\*）**
  - 电脑user里面叫全局，其他是具体项目的里面的约束
  - 更偏向 **“项目完整上下文 + 分层规则 + 技能（skills）”**
  - `skills/` 更像可执行工作流（要手动 `/` 触发）
  - `rules/` 是常驻规则
- **Windsurf（.windsurf/rules）**
  - 更纯粹是 **“全局行为约束（rules）”**
  - 没有单独 `skills` 目录，复杂流程也写在规则里
  - 分 **global_rules（全局） + workspace 规则（项目）**

# Claude Code的使用

#### 注意：修改配置文件后需要重启

- `/`：说明要调用某些指令/模式（本质是给模型注入不同的系统上下文，让它用不同方式解决问题）

- `/init`
  - 初始化项目上下文，通常会出现**多次 HTTP 请求**
  - 原理（现象层面）：一次请求把信息发给大模型，大模型会“指挥”本地去调用工具（例如读文件/搜索/运行命令等），本地把工具结果再回传给大模型，于是形成多轮往返

- `@`：指定文件（限制扫描范围、节省 token、定点修复）
  - 常见场景
    - 你不想它每次都扫描整个项目（即使它不会扫 `node_modules`，大项目也会很耗时/耗 token）
    - 你发现某个文件里有 bug，希望它**只聚焦这个文件**进行分析与修改
  - 用法
    - 在对话/指令里带上 `@文件名` 或 `@相对路径`，让它把该文件当作主要上下文
    - 在本笔记里：`@[6.md]` 就是在引用指定的文件

- `/plan`：计划/思考总结模式
  - 用法：输入 `/plan` 后**不要回车**，直接输入你的需求（有的编辑器里可用 `Shift + Tab` 补全/切换到该模式）
  - 什么时候用
    - 只是简单生成一段代码或小修小改：默认模式即可
    - 需要按步骤完成一个整体需求（多文件改动、设计方案、排查复杂问题）：更适合用 `/plan`
  - 总结：很多编程 AI 客户端本质上都有“默认（直接干）”与“plan（先规划后执行）”两类模式；差异主要来自注入给模型的上下文不同

- 抓包工具（理解 Claude Code 的请求/上下文/工具调用）
  - 目的：抓取 Claude Code 发出的 HTTP 请求，通过请求与响应数据理解它如何组织消息、如何把工具结果回传给模型
  - Windows 桌面端常用：`Fiddler`（Fiddler Classic / Fiddler Everywhere）
    - 平时有空就学习下
      - https://www.bilibili.com/video/BV1Vjs8znEg3?spm_id_from=333.788.player.switch&vd_source=aa34a1eb725d368e4c7d084ae47ffa63&p=4
  - 怎么看
    - 通常看最后一次请求就能理解“最终对模型说了什么”，因为前面轮次的消息往往会被记录到后续请求里当作上下文
    - 一般重点看响应里的 `messages`
      - 把 JSON 复制到 VS Code 新建的 `.json` 文件里查看结构
      - 通过 `role` 判断消息来源
        - `assistant`：大模型回复
        - 工具执行结果：常见是某些 `role` 或字段以 `result` 结尾/带有工具结果的结构（具体字段名会随实现变化）



### claude code开发使用

#### 1）常用内置指令（我们之前已经用过）

- `/clear`：清空当前对话记录（减少上下文干扰/重新开始）
- `/cost`：查看本轮对话已经消耗的 token/费用信息（用于控制成本）
- `/plan`：按计划模式执行需求（适合复杂任务，先拆解再推进）

#### 2）自定义指令（commands）

- 思路：自定义一个指令，本质就是准备一段固定的上下文（通常写在一个 `.md` 里），当你输入该指令时，把这段 md 内容一起发给大模型
- `:run` 这类写法的含义（常见命名约定，不是“官方固定语法”）
  - `xx:run` 可以理解为
    - `xx`：一个指令的“命名空间/分组”（同一类能力归到一起，比如都和 Vue 组件生成有关）
    - `run`：该分组下的“子动作/子命令”（例如 `run`/`test`/`explain`/`refactor`）
  - 作用
    - 让一套命令更好记、更体系化：`/xx:run` 负责“生成/执行”，`/xx:test` 负责“补测试”，`/xx:explain` 负责“解释/教学”
    - 便于多人协作约定：看到命名就能猜到执行意图
  - 可能的落盘方式（不同实现可能不同）
    - 直接用文件名：`.claude/commands/xx:run.md`
    - 用目录分层：`.claude/commands/xx/run.md`（对外仍映射成 `/xx:run` 或 `/xx run`，取决于客户端怎么做映射）
- 位置（全局）：用户目录下的 `.claude`
  - 找到/创建 `commands` 文件夹
  - 在 `commands` 下新建一个 `.md` 文件（或按指令名建文件夹），文件名通常就是指令名
  - 之后你的 Claude Code 就会多一个同名指令
- 全局 vs 项目级
  - `user` 目录下的 `.claude`：**全局生效**（`claude.md` 以及 `commands` 都是全局的）
  - 如果不想全局影响：可以在**项目根目录**下新建一个 `.claude`，这里面的内容会作为**项目专属**配置/指令

#### 3）关于 spec 模式（思路/定位）

- 现状：`spec` 模式很多时候是通过开发者自己写的自定义指令实现的（并非所有环境都“官方内置”）
- 观点：`spec` 也不是“最佳答案”，更重要的是根据项目特点把需求说清楚，然后不断迭代修正
- 一个常见 spec 工作流产物（示例）
  - `require.md`：需求文档（要做什么、预期效果）
  - `technical.md`：技术方案（怎么做、用什么技术/架构）
  - `tasks.md`：任务拆解（拆成可执行步骤/检查点）
- 交互方式：先生成文档 -> 让用户确认/修改 -> 再进入编码 -> 工具写入代码 -> 继续根据上下文判断是否完成

#### 4）自己开发一个“类似 Claude Code”的核心机制

- 角色分工
  - 大模型：负责规划/决策（下一步调用哪个工具、要读哪些文件、要改哪里）
  - 本地 Agent：负责真正执行工具调用，把结果回传给模型，形成循环
- 必须准备的 function tools（这些是 Claude Code 内置的一类能力，你自己做也需要）
  - 读文件：read file（读取指定文件内容）
  - 写文件：write file（创建/覆盖写入）
  - 编辑文件：edit/patch（小范围增量修改）
  - 搜索：grep/search（项目内定位关键代码）
  - 运行命令：run command（跑测试/构建/格式化/启动服务）
  - 向用户确认：ask user（关键决策点收集输入，避免误改）
- 运行方式（最简循环）
  - 把用户需求 + 当前对话上下文发给大模型
  - 大模型输出：要调用哪个 tool + 参数
  - 本地执行 tool，把 `result` 回传给大模型
  - 重复以上过程，直到大模型判断任务完成





# 调教上下文

### 1. 上下文都有哪些？

在 AI 辅助编程（如 Claude Code）中，发送给大模型的上下文主要由以下 4 部分组成：

- **[基本规则]**：System Prompt（系统提示词），这是 Claude Code 客户端官方固定写死的，规定了基本的 Agent 行为。
  - 你可以在用上面那个爬虫软件获取到LLM返回的数据中看到前几条LLM返回的记录中有这个

- **[本次对话记录]**：本次会话的所有问答历史。Claude Code 会自动记录，还可以通过特定指令找回之前的对话继续聊。
- **[自己的特殊规则]**：通过 `CLAUDE.md` 写入的项目全局规则。现在还支持**针对特定文件或文件夹的特殊匹配规则**（通过 rule 文件）。
- **[用户记忆]**：Claude Code 的 Memory 记忆体系。

---

### 2. 恢复之前的对话：`/resume`

- **[基本指令]**：`/resume`
- **[保存位置]**：之前的对话记录都保存在用户目录（User Home）下的 `.claude/project` 文件夹里。
- **[核心机制]**：对话历史是**跟着项目走**的。
  - 意味着你在 `vue-project` 下运行 `/resume`，只能找回你之前在这个项目（`vue-project`）下进行的对话，无法跨项目找回。

---

### 3. Claude Code 的记忆系统（Memory）

传统的 AI 编程工具往往会全自动在后台偷偷总结所有对话生成记忆，而 Claude Code 的 Memory 体系则截然不同：

- **[显式触发]**：它需要你**在对话中明确命令它记忆**，它才会记录。例如：“*请记住，我们在本项目中使用 Element Plus 库*” 或 “*针对刚才的实现生成一下记忆*”。
- **[按需生成]**：它仅根据你的明确指令以及当前对话的内容去提炼并生成 Memory，**绝对不会自动总结所有对话**，避免引入无用或冲突的干扰信息。
- **[项目绑定]**：记忆也是跟着项目走的。在 Claude Code 的世界里，一个项目目录就对应着一个独立的沙盒。你的对话历史和 Memory 都是项目隔离的。

---

### 4. 针对特定文件匹配的规则（Rule的定义与 Glob 匹配）

除了在项目根目录下写一个全局的 `CLAUDE.md`，我们还可以针对**特定文件或文件夹**去定制一些特殊的 rule。

#### 1) 它的工作原理是什么？
- 当你在对话中使用 **`@文件名`** 指定某一个文件时，Claude Code 客户端会在**本地**检测此文件路径，并寻找命中匹配规则的 rule，将该 rule 的说明内容作为上下文一起拼接发送给模型。
- **注意**：这个匹配动作是由 **Claude Code 客户端在本地做 `@` 解析时完成的**，并不是云端 AI 模型自己发现的。
  - **[具体案例剖析]**：
    - 假设你在对话里输入：*“请帮我在 **@xx.vue** 这个文件中生成一个弹窗”*
    - **xx.vue 代表什么**：它是你项目中**具体存在的某一个代码文件**。
    - **背后发生的过程**：
      1. 当你加上 `@xx.vue` 后，Claude Code 客户端会在本地检索出这个文件的实际物理路径（例如 `src/components/xx.vue`）。
      2. 客户端匹配本地规则：它发现该路径命中了规则定义（比如你定义的 `src/components/**/*.vue` 规则）。
      3. 自动打包发送：客户端会自动把你设定的关于“Vue 组件规范”的 rule 规则、`xx.vue` 的实际代码内容，以及你的诉求一并打包，发送给大模型。
      4. 大模型最终会**无缝适应并严格遵守对应的规则**，在 `xx.vue` 这个具体的文件中，为你生成百分之百符合开发规范的弹窗代码。

#### 2) 是否必须要 `@` 开头才行？（`@` 的核心价值）

**结论**：**不一定非要用 `@`**，直接说名字大模型也能找到，但 **强烈推荐并且尽量使用 `@` 开头**。两者的运作机制和效率有天壤之别：

- **[情况 A：不用 `@`（只提文件名，如：“修改 xx.vue”）]**：
  - **大模型自己找工具**：你的第一轮 HTTP 请求只发送了纯文本。大模型拿到后，意识到需要找 `xx.vue` 这个文件。
  - **多轮交互延迟**：大模型需要向本地发出调用“搜索文件”的工具指令，本地 Agent 查到路径后回传给大模型，大模型再发出“读取文件”的工具指令，本地读取后再回传……
  - **规则无法匹配**：最致命的是，因为你没有使用 `@xx.vue`，**Claude Code 客户端在第一阶段不会去做本地 Rule 匹配**！大模型在开始工作前拿不到专属的匹配规则，容易写出不符合你特定规范的代码。

- **[情况 B：使用 `@` 开头（如：“修改 @xx.vue”）]**：
  - **一步到位预加载**：客户端在发出第一条请求前，就在本地把 `xx.vue` 的内容读好了，并且**自动检索并合并了对应的局部 rule（如 Vue 组件规范）**。
  - **省 Token 与回合**：首轮请求大模型时，它手里就已经拿齐了“具体代码 + 对应规则”，省去了多次工具调用的往返网络延迟，响应快、极省 Token。
  - **规则百分百生效**：只有加上 `@`，客户端在本地进行 `@` 解析时，才会触发 Glob 规则的完美自动匹配。

---

#### 3) 通配符（Glob 路径）写法（规则匹配语法）
在规则文件的头部，我们使用 YAML Frontmatter 来声明此规则对哪些路径生效：
- `**/*.css`：代表项目里**所有**层级目录下的 `.css` 文件。
- `src/**/*`：代表 `src` 目录下的**所有**文件。
- `src/*.vue`：代表 `src` **一级目录下**的所有 `.vue` 文件（不包含子目录）。
- `src/**/*.vue`：代表 `src` 下的**所有** `.vue` 文件（包含所有子目录）。

---

### 5. 匹配规则（Rule）在 Claude Code (Codex) 和 Windsurf 里的路径与写法对比

假设我们有一个名为 `vue-project` 的项目，现在想针对项目中的 Vue 文件添加一套专用的编码规范 rule。

#### 1) 在 Claude Code (Codex) 中
- **[完整文件路径]**：`vue-project/.claude/rules/vue-style.md`
  - *(注：如果是全局生效，则放在用户主目录下的 `~/.claude/rules/vue-style.md`)*
- **[Frontmatter 写法]**：
  ```markdown
  ---
  paths:
    - "src/**/*.vue"
  ---
  # Vue 样式与组件编写规范
  
  - 必须使用 <script setup> 语法
  - 样式必须加 scoped 属性
  ```
  *(注意：在 Claude Code 中头部的关键字通常使用 `paths` 数组)*

#### 2) 在 Windsurf (Cascade) 中
- **[完整文件路径]**：`vue-project/.windsurf/rules/vue-style.md`
  - *(注：项目级规则也可以直接写在根目录下的全局规则文件 `vue-project/.windsurfrules` 里面)*
- **[Frontmatter 写法]**：
  ```markdown
  ---
  globs:
    - "src/**/*.vue"
  ---
  # Vue 样式与组件编写规范
  
  - 必须使用 <script setup> 语法
  - 样式必须加 scoped 属性
  ```
  *(注意：在 Windsurf 中头部的关键字使用 `globs` 数组来指明作用的文件范围)*

#### 3) YAML Frontmatter 格式提示
不管是 `paths` 还是 `globs`，格式必须严格遵守 YAML 的 frontmatter 写法（用前后三个短横线 `---` 包裹起来）：
```yaml
---
paths: (或 globs:)
  - "src/**/*.vue"
  - "src/**/*.js"
---
```





# 添加和配置MCP

### 1. 核心概念与原理回忆

Model Context Protocol (MCP) 是连接大模型 Agent 与外部工具/数据源的标准桥梁：

- **[三大连接类型]**：MCP 协议目前支持以下三种连接传输方式，每种方式的通信机制不同：
  - `stdio`（标准输入输出）：最常见、最简单的类型。Agent 作为父进程启动本地 MCP 服务的可执行命令，通过 `stdin`/`stdout` 与子进程进行 JSON-RPC 通信。
  - `sse`（Server-Sent Events）：主要用于远程服务器连接。通过 HTTP 请求发起，由服务端通过 SSE 流式向客户端推送事件与工具列表，适合云端或跨域调用。
  - `streamablehttp`（流式 HTTP）：通过 HTTP 请求响应进行流式数据传输，适合特定的网络通信场景。
- **[Agent 底层加载原理]**：
  - 我们自己开发 Agent 时，需要在配置文件中声明需要连接的第三方 MCP 服务列表。
  - 每次 Agent 启动时，都会**遍历这些配置**建立连接。
  - 连接成功后，Agent 从各个 MCP 服务端获取其支持的 **function tools 列表**，将它们动态“混入”到 Agent 原生内置的工具列表（如 read, write 等）中，一并提供给大模型调用。大模型就可以像调用内置工具一样，自由调用外部 MCP 工具。

---

### 2. 在 Claude Code 中连接和配置 MCP

在官方的 Claude Code 客户端中，主要有命令行添加与手动修改配置文件两种方式：

#### 1) 命令行安装
通过 `claude mcp add` 指令来动态添加：
```bash
claude mcp add --transport <mcp类型> <mcp名字> [--scope user/project] <mcp地址/或者启动命令>
```
- **`--transport`**：指定传输协议类型，通常是 `stdio` 或 `sse`。
- **`--scope`**：作用域。这里传入的 `user` 或 `project` 是**系统固定的内置关键字**（不能改写成你的实际用户名或实际项目名）：
  - `user`：固定值，代表全局（Global）配置。
  - `project`：固定值，代表当前项目（Project）配置。
  - *系统根据这两个固定关键字来决定把配置信息写入哪个目录下的配置文件。*
- **`[启动命令示例]`**：
  - **如果你想全局可用（写入全局 `~/.claude.json` 中）**：
    ```bash
    # 显式指定为全局作用域（默认不写 --scope 时，也是 user 全局生效）
    claude mcp add --transport stdio my-db --scope user npx -y @modelcontextprotocol/server-postgres "postgresql://localhost/mydb"
    ```
  - **如果你只想在本案例项目里生效（写入当前项目级配置中）**：
    ```bash
    # 指定作用域为 project，仅会在当前目录下打开 Claude Code 时加载这个 MCP 工具
    claude mcp add --transport stdio my-db --scope project npx -y @modelcontextprotocol/server-postgres "postgresql://localhost/mydb"
    ```

#### 2) 手动直接改文件

- **[基本原理]**：命令行添加的 MCP，本质上只是被客户端转换成 JSON 并写入了配置文件中。因此，我们自己直接创建/修改配置文件，效果完全一样。
- **[全局配置案例 (User Global)]**：
  - **文件路径**：
    - Windows 端：`C:\Users\你的用户名\.claude.json` (或简写为 `~/.claude.json`)
    - Mac / Linux 端：`~/.claude.json`
  - **配置文件内容案例**：
    ```json
    {
      "mcpServers": {
        "my-db": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://localhost/mydb"]
        }
      }
    }
    ```
- **[局部项目配置案例 (Project Local)]**：
  - **文件路径**：
    - `vue-project/.claude.json` (在项目根目录下，开始文件夹为 `vue-project`)
      - 这个文件下除了配置 MCP 之外，针对**具体的文件夹/特定文件模式**也可以设置对应的 rule 限制，以及指定扫描忽略项。比如下面的projectRules
  - **配置文件内容案例（包含文件夹级 Rule 与 忽略项）**：
    ```json
    {
      "mcpServers": {
        "my-db-local": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-postgres", "postgresql://localhost/mydb_local"]
        }
      },
      "projectRules": [
        {
          "globs": ["src/components/**/*.vue"],
          "rules": "所有的公共组件必须符合 Vue3 <script setup> 规范，必须带有 scoped 样式，且需要增加 TypeScript 定义"
        },
        {
          "globs": ["src/store/**/*.ts"],
          "rules": "状态管理相关的修改，必须优先遵循 Pinia 最简组合式 API 写法，禁止使用过时的 Vuex"
        }
      ],
      "ignorePatterns": [
        "dist/**",
        "public/static/**",
        "**/*.log"
      ]
    }
    ```
    - **`projectRules`**：可以通过 `globs` 锁定具体文件夹或特定文件格式，并为其注入专属的 `rules` 调教，实现文件夹级的“定点规范约束”。
    - **`ignorePatterns`**：用来排除不需要扫描、不需要作为上下文的巨大文件夹或临时日志，能省下巨额的 Token 和初始化扫描时间。

---

### 3. 在其他 AI 客户端中的配置 (如 Windsurf, Cursor)

如果你不使用 Claude Code 终端，而是使用其他的 AI 编辑器或客户端，机制也是相通的：

- **[Cursor 中的配置]**：通过 Cursor 的 Settings -> Features -> MCP 界面进行可视化添加。它本质上也是维护了 Cursor 的本地配置文件，你可以指定 `stdio` 的 command 与 args 或 `sse` 的 URL。
- **[Windsurf (Cascade) 中的配置]**：
  - 在 Windsurf 中，Cascade 同样支持导入第三方 MCP。
  - 要么在 GUI 的设置中添加，要么直接改配置文件（如 `~/.codeium/config.json` 或 `.windsurf` 的底层配置），将其对应的 MCP 服务注册进去。
- **[统一的规律]**：不管是哪种 AI 客户端，连接第三方 MCP 的本质都一样：**要么命令行/GUI 添加间接改文件，要么你直接去改配置文件**。

---

### 4. 优秀的 MCP 工具资源市场与官方列表

为了让 Agent 拥有更强大的外部能力，可以从以下专门的 MCP 仓库和社区寻找开箱即用的工具：

- **[MCP.world]**：`https://mcp.world/`
  - 这是一个专门收集、分类和推荐各种高质量第三方 MCP 服务的社区平台。你可以直接在此搜索如“谷歌搜索”、“GitHub 交互”、“浏览器爬虫”等实用 MCP 并获取一键安装命令。
- **[Glama MCP Registry]**：`https://glama.ai/mcp/registry`
  - 提供了极佳的可视化 MCP 服务列表和检索，支持按分类（数据库、开发工具、搜索、通知等）探索社区开源的工具节点。
- **[Anthropic 官方开源服务器集]**：`https://github.com/modelcontextprotocol/servers`
  - 官方维护的参考开源 MCP 仓库。里面包括了非常经典的数据库（Postgres、Sqlite）、Google Maps、Memory、Filesystem、Puppeteer 等可以直接安装即用的官方核心 MCP 服务。





# Skill（智能技能/自定义工作流）

### 1. 为什么要使用 Skill？（解决什么痛点？）

假设我们在开发中遇到了一个典型场景：我们公司前端有一套极其独特的 Vue3 组件规范（包括特定的弹窗设计、表格封装、以及公司专属的 UI 审美体系等），我们该怎么让 AI 遵循？

- **[痛点 A：写在全局 `CLAUDE.md` 中]**：
  - 虽然可行，但大模型**每次**对话和提问都必须全量带上这堆长篇大论。这会造成上下文臃肿，极其**浪费 Token 成本**。
- **[痛点 B：写在 `rules` 规则文件中]**：
  - Rule 只适用于非常**死板且明确**的文件类型或文件夹匹配（如 `src/**/*.vue`）。面对一些逻辑复杂、业务抽象的非标流程（例如：特定重构步骤、自定义测试指令流水线等）它无能为力，且必须要用户显式输入 `@文件名` 才能生效。
- **[Skill 的完美解决方案]**：
  - Skill 是一套**由大模型根据你的诉求，智能决定是否按需加载**的超级提示词（或工作流）系统。
  - 只有在模型发现当前任务“非常合适”该 Skill 时，才会把完整详情加载进上下文，平日里则只占用极少的 Token。

---

### 2. Skill 的底层工作原理与智能流程（核心）

Skill 的最大魅力在于其“按需懒加载”的套件流机制：

1. **[第 1 阶段：轻量初始化]**：
   - 当 Claude Code 客户端启动时，会自动扫描所有的 Skill，但**仅提取其名字（name）和描述（description）**。这部分极其轻量。
2. **[第 2 阶段：名字与描述载入]**：
   - 客户端只把所有 Skill 的“名字和简短描述”这一小段带入首轮 HTTP 请求的上下文中。
3. **[第 3 阶段：大模型智能判断]**：
   - 大模型拿到你的实际需求后，会在脑海中遍历这堆 Skill 的简短描述。它会自主评估：“*针对用户当前要解决的问题，我是不是可以调用某个 Skill 来提效？*”
4. **[第 4 阶段：动态拉取详情]**：
   - 一旦大模型决定使用某个 Skill（例如：`vue3-standards`），它会向本地的 Claude Code 客户端发起请求：“*请把 `vue3-standards` 的具体完整内容发给我*”。
5. **[第 5 阶段：严格执行]**：
   - 客户端将该 Skill 的具体 MD 详情喂给模型，大模型随后开始严格遵照 Skill 里的具体执行细节、步骤和代码规范进行开发。

---

### 3. Skill 的目录结构与创建位置

不管是全局还是具体项目，都可以放置专属的 Skill：

- **[完整路径规范]**：
  - **全局生效**：`~/.claude/skills/【具体技能子文件夹】/skill.md`
  - **项目级生效**：`vue-project/.claude/skills/【具体技能子文件夹】/skill.md`（开始文件夹自项目如 `vue-project` 开始）
- **[必遵硬性规则]**：
  - 1. 必须是 `.claude` 目录下的 `skills` 文件夹里。
  - 2. 在 `skills` 目录下必须**建一个子文件夹**来承载每个独立的 Skill。
  - 3. 子文件夹下**必须建立一个名为 `skill.md` 的入口文件**，用来书写规则（格式必须为全小写）。

---

### 4. 描述头（YAML Frontmatter）书写规范

在 `skill.md` 的最头部，必须用 YAML 头来进行元数据标记。**请注意：所有的冒号 `:` 和对应的值之间，必须空一个英文空格**。

```markdown
---
name: 技能的名字 (核心，必须)
description: 简短的描述，用来让模型决策是否加载该技能 (核心，必须，需写明适用场景)
license: 开源许可协议 - 比如 MIT (可选)
metadata: 供你随意拓展的额外自定义数据 (可选)
allowed-tools: ['read_file', 'write_to_file'] /* 运行此技能允许调用的工具列表，可选 */
---

# 这里往下写具体的规则、工作流步骤、规范细节...
```

- **[书写细节避坑]**：
  - ```yaml
    # ❌ 错误写法（无空格）：
    name:my-custom-skill
    #  正确写法（有一个空格）：
    name: my-custom-skill
    ```

---

### 5. 典型案例：高级非标工作流（用 Skill 还是 Function Tool？）

让我们深入前面的 TXT 转 MD 案例，看看利用 **Skill** 和 **Function Tool（写 MCP）** 解决这一非标复杂诉求的巨大差别：

- **[方案 A：使用 Function Tool（开发 MCP 服务）]**：
  - **局限**：你必须专门写一套 Node.js/Python 脚本来实现这一工具，然后遵照 Model Context Protocol 标准编写一个 MCP 服务，通过本地或线上发布后，再配置导入到 Claude Code 中。
  - **成本**：开发门槛高、逻辑方案重、不便于快速迭代。
- **[方案 B：使用 Skill 逻辑解析内嵌代码（推荐）]**：
  - **做法**：直接在 `skill.md` 中说明，当用户想把 `.txt` 转换为 `.md` 时，禁止用原生的读写工具操作，而是指示大模型去“参考并调用我为你预留的专用代码模板来执行”。
  - **底层原理（AI 怎么执行 Skill 里的代码？）**：
    1. 在 `skill.md` 详情中，你写明什么场景下运行某段代码，或者运行某个命令。
    2. 你的代码和命令里可以**预留一些变量占位符**（就像 `function tool` 的参数一样），让 AI 结合当前对话的实际场景去替换。
    3. AI 决定调用该 Skill 后，会自动根据你的代码模板，**动态填入需要修改的具体参数**（如实际的文件路径、参数值等）。
    4. **代码执行机制与链路**：
       - **简单逻辑**：AI 会直接在本地通过运行一行 `node -e "大模型生成的代码"` 命令来快速执行。
       - **复杂逻辑**：若代码量大、没法直接单行运行，AI 会在本地**自动将大模型填充后的完整代码写入为一个临时文件并运行**。
       - **执行完后清理**：在临时代码执行完毕后，客户端会**自动删除该临时文件**，保持你工作区干净。
- **[思想突破：Skill 理论上可以给 AI 增加一切能力]**：
  - 因为 Skill 内部支持直接内嵌并运行代码，而 AI 又能够根据你提供的代码模板智能调整并执行。
  - **这意味着：你根本不需要写繁琐的自定义 Function Tool，仅靠 Skill 就能教 AI 掌握一切技能！**
  - **经典设想 1**：给 AI 一个 Skill，告诉它如何安全连接、授权并直接查询你们公司的私有数据库。
  - **经典设想 2**：给 AI 一个 Skill，教它如何利用你们公司的专有加解密算法去读写那些特殊的加密文件。

---

### 6. Skill 与 Function Tool (MCP) 深度对比

| 对比维度 | Skill（智能技能） | Function Tool（MCP 外部工具） |
| :--- | :--- | :--- |
| **难易程度与拓展性** | **极其简单**。无需懂得复杂的开发规范，写个 `skill.md` 就能扩展能力，拓展性极佳。 | **较为复杂**。需要掌握完整的 MCP 协议开发规范并进行发布，成本大、方案重。 |
| **执行稳定性** | **有偏移风险**。大模型在理解上偶尔会跑偏，可能没有完美按照你的代码运行，或者代码执行失败。 | **高稳定性**。AI 仅负责传入参数，后台代码由本地程序死板控制，稳定性极佳，AI 调用积极性也高。 |
| **客户端适配度** | **有局限性**。目前仅支持内置 Skill 体系的 AI 客户端（如 Claude Code）。 | **通用性强**。属于标准 MCP 协议，几乎所有主流大模型与 AI 编辑器都支持。 |

---

### 7. 避坑与注意事项：代码并不一定是用来“运行”的

你在 `skill.md` 里写的大段参考代码、脚本，**大模型并不一定每次都会傻傻地去执行它**。

- **[核心常识]**：**很多时候，我们写代码块只是为了给 AI 说明业务逻辑**。
- **[逻辑范例]**：把这些代码块作为“伪代码”或者“示例 Demo”喂给 AI，能够极大地启发大模型。大模型在看懂你的代码逻辑后，能在不运行它的情况下，完美写出符合该逻辑的其他配套代码。

---

### 8. Skill 市场：开箱即用的生态组合

作为极具革命性的智能 Agent 能力扩展生态，Skill 拥有自己专属的社区共享市场。你可以直接在这里找到各种其他人写好的高级 Skill 提示词和工作流：

- **[Skill 市场官方网址]**：`https://skills.sh/`
- **[生态特色]**：在这个平台上，你可以搜索或分享各种大牛调教好的特殊技能。比如：自动化架构检查、特殊框架模版生成等，可以直接引入到我们本地。

---

### 9. Skill 的轻量级发布与远程拉取安装

区别于开发 MCP（必须租用服务器、部署接口或发布至重的 `npm` 市场），**Skill 极其轻量，特别适合非专业开发人员发布和分享**：

- **[超轻量发布步骤]**：
  1. 你只需要把包含 `skill.md` 技能详情的文件夹**上传到任何支持 Git 协议的托管仓库**（如 GitHub, GitLab, Gitee 等）。
  2. 托管仓库是完全公开和开源的。
- **[远程一键拉取安装]**：
  - 市场上目前有成熟的集成工具，允许你直接用命令行在本地终端一键拉取他人的 Skill。
  - **安装命令**：
    ```bash
    npx skills add <Git仓库地址> --skill <仓库中具体skills文件夹下对应的某个skill名字>
    ```
  - 这行指令执行后，客户端会自动抓取远端 MD 规则并写入你的本地配置中，极为灵活。

---

### 10. 判断 Skill 或 Rule 相关文件是否创建成功

当你新建了 Skill 或 Rule，有以下两种直观的方法去验证它是否已经成功被客户端加载：

- **[方法 A：斜杠弹出检查]**：
  - 在 Claude Code 终端命令行里直接输入一个斜杠 `/`，然后仔细观察下拉弹窗列表中。如果出现了你刚才设置的 Skill 或 Rule，代表它已经顺利进入待命状态。
- **[方法 B：首轮发送确认]**：
  - 在终端中敲入任意指令，让它开启本次会话。若读取成功，Claude Code 的初始化控制台日志中，会有绿色的提示字符：`Successfully loaded skill: <你的技能名>`。





# skill开发小实战

自定义 Skill 不仅是书写一个规则文件，更是为 AI 装配一个拥有极强业务属性的“技能套件”。在开启实战前，我们需要先理清它的应用维度：

### 1. Skill 的三大核心使用场景

- **[约定规范约束]**：类似于 `CLAUDE.md` 或 Rule，但比 `CLAUDE.md` 更省 Token，比 Rule 更加灵活且作用域更广。当大模型产出的代码不符合你的团队要求时，在 Skill 中订立强约束让其无缝适应。
- **[拓展全新能力]**：当 AI 客户端本身做不了某件特殊的、深度的、非标准的事情时（例如特定格式的解密、调用极其冷门的本地编译器、特殊数据源比对等），可以用 Skill 手把手教它怎么做。
- **[定义自动工作流]**：将我们平常繁琐的、需要手动执行的多步骤操作，在 Skill 中说明完整步骤与执行流程。让 AI 代替我们自动完成**“多工具组合套件流水线”**。

---

### 2. Skill 的三种内容呈现形式

编写 `skill.md` 时，我们可以交替或混合采用以下三种内容进行引导：

- **[纯文本内容]**：提供步骤描述、规范条例与决策逻辑，说明应该怎么做、步骤该怎样。
- **[代码片段 (模板/样例)]**：当文本说不明白抽象的逻辑时，通过贴入代码块进行说明，或者**直接提供代码模板供 AI 动态填充并运行**（AI 会根据场景将代码装载为临时文件运行，执行后自动清理）。
- **[Bash 终端指令]**：如果我们希望 AI 完成的事情可以通过一系列 Shell 命令达成，只需在 Skill 中教 AI 如何在终端运行对应的命令行组合。AI 拥有运行系统命令的 tool，可以百分之百替你执行。

---

### 3. 实战背景：公司 40+ 项目管理与自动克隆、提交记录流水线

#### 1) 开发者痛点
- 公司里有大大小小 40 多个项目，各自有动态组件库、内部 JS 库、子模块等。每一个项目都是一个独立的 Git 仓库。
- 每次新加入一个开发需求时，开发者都要费尽心思去找“这个项目的 Git 仓库地址在哪里”、“先前的开发文档放在了哪”、“我本地到底有没有拉取过这个项目代码”。

#### 2) 王炸解决方案：设计一个“项目一键初始化与自动开发日志” Skill
我们希望能开发一个 Skill：
1. 当我们需要开发某个项目时，只需对 AI 说：*“我要开发 xxx”* 或者描述需求（例如：*“我要去重构动态表单那个项目”*）。
2. AI 接收到指令后，**智能判定匹配出具体是哪一个项目仓库**。
3. AI 自动检测我们指定的**本地代码放置文件夹**下是否已经存在该项目。
4. **如果本地已存在**：AI 会自动帮我们转入（或打开）本地项目目录。
5. **如果本地不存在**：AI 会自动通过 `git clone` 远程地址把项目拉到本地。
6. **项目拉取下来后**：AI 自动读取项目根目录下的 `readme.md` 或 `changelog.md`；并在后续我们开发修改、完成代码提交后，**自动把本次提交的修改描述追加写入到 `readme.md` 的开发日志中**。

---

### 4. “项目管理与拉取” Skill 的逻辑解析与实施流程

要想让这个 Skill 顺畅运转，AI 的思考与执行链路如下：

```
                    【用户输入：“我要重构表单组件项目”】
                                    │
                                    ▼
       【第一步：准备/拉取公司的项目映射索引文档（包含名字、Git地址等）】
                                    │
                                    ▼
                 【第二步：AI 语义理解匹配，判断是哪个具体项目】
                                    │
                                    ▼
           【第三步：AI 获取本地存放目录，自检本地是否存在该项目】
                                    / \
                                  有   没有
                                  /     \
                                 ▼       ▼
                       【直接打开本地项目】 【运行 git clone 远程拉取】
                                                │
                                                ▼
                                    【打开根目录 readme.md】
                                                │
                                                ▼
                                  【提交代码时自动追加开发日志】
```

- **[第一步：建立项目映射索引]**：
  - 在本地或云端在线文档中准备一个表格/ Markdown，清晰记录：**“项目名字 ｜ Git仓库远程克隆地址 ｜ 项目核心功能与描述”**。
- **[第二步：AI 拉取并匹配项目（如何读取文档？）]**：
  - **本地文档方案**：如果文档存在本地，在 `skill.md` 中把该文件的路径直接写死，告诉 AI 文件存放在哪（如 `C:/Users/MJL/Desktop/projects_map.md`）。AI 启动后会自动调用 `read_file` 去读这个索引，并做语义匹配。
  - **在线文档方案**：如果放在云端协同文档，可以让 AI 借由 MCP 在线文档连接器（如语雀/腾讯文档 MCP）或者自写接口脚本，发起 HTTP 请求拉取文档内容。
- **[第三步：本地文件夹检测与拉取]**：
  - 在 Skill 中告诉 AI，我们的代码一般存放在哪个目录。
  - AI 会首先调用本地目录检测工具，去排查该目录下有没有对应的项目文件夹。
  - 若检测失败，AI 会直接生成 `git clone <第一步里匹配出来的 Git 地址>` 并运行。
- **[第四步：readme.md 开发日志自动追加]**：
  - 在 Skill 中下达硬性约束：*“当拉取项目或重构完毕，准备提交代码时，你必须先读取根目录下的 `readme.md`，分析本次改动并在其末尾的 `# 开发日志` 部分追加一条结构化的更改记录（包含日期、作者、本次修改的核心模块及改动原因），之后再执行代码提交。”*

---

### 5. 吐槽与避坑：云端在线文档 MCP 的现状与降级方案

如果你选择上面的 **“在线文档拉取方案”**，在实际开发连接器时，有以下几个极其难受的坑需要注意：

- **[痛点 A：语雀在线文档 MCP]**：
  - 语雀官方目前要求**必须开通超级会员**才允许用户创建个人 API Token。这给免费个人开发者筑起了高墙。
  - **优雅降级方案**：民间有一些第三方的开源语雀爬虫 MCP，它允许你**手动将浏览器的 Cookies 复制过去**。MCP 内部通过模拟请求（爬虫网页解析）的方式直接越过 Token 限制拉取文档内容。
- **[痛点 B：腾讯文档 MCP]**：
  - 腾讯文档官方提供的官方文档定义常常模糊不清，接口频繁更新，很容易导致配置好的 MCP 频繁报错或失效。
  - **优雅降级方案**：与其等模糊不清的官方 MCP，更推荐走**开发者调用腾讯文档接口 API 形式**。利用 Skill 里的临时代码执行特性，让 AI 自己去拼装腾讯文档的 API，直接去拉取在线腾讯文档的数据。

---

### 6. 实战落地：核心 `SKILL.md` 源码文件结构与操作步骤

为了让这两个核心业务能力真正跑通，下面是两个落地部署的 `SKILL.md` 真实源码配置模板，建议直接收藏参考：

#### 1) 实战案例一：`mygit-skills/SKILL.md`（高标准 Git 提交规范与自动日志追加）
在 `.claude/skills/mygit-skills/skill.md` 中写入以下配置：

```markdown
---
name: mygit-skills
description: 当用户让你进行git提交的时候，一定要遵循此skill的规则
---

# 操作步骤

1. 通过 `git add .` 把所有的文件加入暂存区。
2. 如果缓冲区内还有未加入的文件，继续执行第一步。当缓冲区全部处理完毕、没有剩余未暂存文件时，执行 `git commit -m "提交理由"`。
   - **关于理由规范**：提交理由必须严格遵循 `fix/feat/debugger: 理由描述` 格式（`fix` 为 Bug 修复，`feat` 为新增功能，`debugger` 为调试记录）。理由中的文本部分必须由 AI 自动扫描本次修改的实际提交内容并智能总结提炼生成，严禁敷衍。
3. `git commit` 成功后，通过 `git push origin 分支名` 推送到远端。
   - **分支获取与推送逻辑**：AI 必须首先检查用户当前所在的分支。进行确认后，如果所在分支不是 `main` 或者 `master`，则直接把当前所在的分支名作为对应的远端推送目标分支。
4. 前三步全部完成后，把本次提交的提炼理由，按照指定的格式，追加写入到当前项目一级目录下的 `readme.md` 中。
   - **追加日志写入格式**：`**YYYY:MM:DD HH:mm 用户名: 提交理由**`（需置于 `# 开发记录` 模块中）。
```

- **[AI 后台指令翻译执行剖析（它是如何跑通这一套的？）]**：
  当你下达了上面这些描述性指令（如“检查用户当前所在分支确认”），Claude Code 在解析 Skill 详情时，会自行在后台将其翻译为具体的系统底层工具调用：
  - **自动检索分支名**：大模型不会凭空猜测。它会主动在本地终端执行原生指令 `git rev-parse --abbrev-ref HEAD` 或 `git branch --show-current` 来获取你当前所处的分支（如 `feature/login`），然后自动将结果动态拼装到 `git push origin feature/login` 之中。
  - **获取真实的系统/Git用户名**：为了写入 `readme.md` 中的 `用户名`，大模型会智能读取你本地的 Git 全局配置（通过执行 `git config user.name`）或者系统环境变量（如 `process.env.USERNAME`），提取后作为变量拼入。
  - **格式化本地时分戳**：它会利用本地 JavaScript 的 `new Date()` 或系统 `date` 命令行，动态计算并拼接好当前时刻的 `YYYY:MM:DD HH:mm`。
  - *这再次证明了，哪怕我们写的是自然语言步骤描述，大模型也可以借由本地功能工具链，完美转译为严谨的操作系统底层操作。*

#### 2) 实战案例二：`find-project/SKILL.md`（项目智能检索、本地自检与自动克隆）
在 `.claude/skills/find-project/skill.md` 中写入以下配置：

````markdown
---
name: find-project
description: 当需要根据需求或者应用名称找某个应用/项目的时候，阅读此skill
---

# 操作步骤

1. 运行以下原生的 `bash` 命令，拉取公司统一存放的项目记录文档（由于文档在腾讯文档中，我们这里原封不动运行 curl 命令直接调用腾讯文档的 OpenAPI 接口进行无感读取）：
   ```bash
   curl -X GET 'https://docs.qq.com/openapi/doc/v3/300000000$YtvJowvfUAAb' \
     -H 'Access-Token: eyJhbGciOiJIUzI1NiIsIn...' \
     -H 'Client-Id: 48202cedd0294b589...' \
     -H 'Open-Id: be3f7fa2ff3146a199c8b...'
   ```
   *(注：该接口文档内极其详尽地记录了公司的每一个项目、Git远程克隆地址、以及对应的功能描述、相关开发人。**切记：请原封不动地执行此命令，禁止擅自修改任何请求头**)*

2. 成功拉取到文档内容后，根据用户的提问（或诉求描述），进行智能语义解析，判断哪个项目最符合用户的实际要求。
   - **如有多个项目符合需求**：则大模型必须列出表格，让用户手动选择一个，并给出在线项目文档的原始地址。
   - **如只有明确的一个项目符合**：则直接进行下一步。
3. 当确认好要操作的目标项目后，如果 AI 尚未明确你要存放代码的本地主文件夹，必须主动询问用户：“*您的项目通常放在本地哪个文件夹？*”，直到获得明确的本地路径后，才能进行后续步骤。
4. 根据第三步获得的本地主文件夹路径，在本地查找、检索该文件夹下是否已经存在了和目标项目“同名”的子文件夹：
   - **如果已经存在**：无需重复下载，直接使用本地 `vscode`（或对应的 shell 指令）打开该本地项目。
5. **如果本地没有同名文件夹**：则调用 `git clone` 指令，去远程 Clone 下该项目。项目的远程 Git 地址在第一步拉取的文档内容里均有对应说明（注意一律采用 `https` 格式）。
   - **异常处理**：若 clone 失败，大模型应当立即把 Git 远程地址暴露给用户，并提示：“*自动克隆遇到异常，建议您手动执行 git clone <地址>*”，不要自己盲目重试或擅自查找底层原因导致卡死。
6. 项目成功打开后，AI 扫描项目一级目录下是否含有 `readme.md` 文件：
   - **如果有**：直接结束。
   - **如果没有**：则自动从第一步在线文档中拉取该项目的描述与元数据信息，在本地自动创建并写入一份规范的 `readme.md` 文件。
````

---

### 7. 终端命令可靠性避坑：原生 Bash 指令 vs 第三方全局工具

在编写 `SKILL.md` 时，如果涉及到需要让 AI 执行某些终端命令（Bash 指令），我们需要了解 Claude Code 在本地执行环境下的可靠性阶梯：

```
可靠性：高 ────────────────────────────────────────────────────────► 低

【操作系统原生 Bash 内置指令】  ───► 【第三方全局/局部构建指令】
 (如：curl, git, cd, mkdir, rm)      (如：webpack, vite, npm, pnpm)
```

- **[可靠性极大：原生 Bash 常用指令]**：
  - `curl`、`git`、`cd`、`mkdir`、`rm` 等是大部分主流操作系统（Windows/Mac/Linux）中默认且原生集成的。
  - 大模型在调用这些原生指令时，因为不依赖任何第三方的包管理环境（如 Node/Python），几乎能做到 **100% 执行成功率**。所以在 Skill 中调用它们（例如上面的一键 curl 拉取腾讯文档接口）是非常稳定且极其推荐的。
- **[极易失败：第三方全局/局部构建指令]**：
  - `webpack`、`vite`、`npm`、`pnpm`、`yarn` 等指令严重依赖用户本地的环境变量配置、Node 版本的兼容性、以及是否提前在全局全局安装了对应的 CLI 包。
  - 由于大模型运行在一个相对隔离的底层沙盒终端内，经常会遇到环境变量缺失（如 `Command not found: pnpm`）等莫名其妙的本地报错，导致工作流直接卡死。
- **[设计防御性 Skill 规范]**：
  - 在涉及到第三方全局工具的构建 and 运行步骤时，应当在 `SKILL.md` 里增加**“防御性保障逻辑”**：
    - 例如：“*首先自检 pnpm 是否存在，如果报错，应当优雅提示用户手动在终端执行该步骤，严禁死板循环重试*”。这能让你的 Skill 在不同的开发机上都拥有极高的适应性和极佳的鲁棒性。
